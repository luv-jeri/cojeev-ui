{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "text-reveal",
  "title": "Text reveal",
  "description": "A quiet word-by-word entrance for headings and short editorial text, with rise and fade treatments.",
  "dependencies": [],
  "registryDependencies": [
    "https://luv-jeri.github.io/sahajiv-ui/r/sahajiv.json"
  ],
  "files": [
    {
      "path": "registry/sahajiv/ui/text-reveal.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { useMotionVisibility } from \"@/lib/sahajiv-motion/use-motion-visibility\";\n\nexport type TextRevealProps = Omit<React.HTMLAttributes<HTMLElement>, \"children\"> & {\n  text: string;\n  as?: \"span\" | \"p\" | \"h1\" | \"h2\" | \"h3\";\n  /** Change this value to deliberately replay the reveal. */\n  replayKey?: string | number;\n  variant?: \"rise\" | \"fade\";\n  duration?: number;\n};\n\n/** A single, bounded word reveal. The complete text remains readable without motion or JavaScript. */\nexport function TextReveal({ text, as: Tag = \"span\", replayKey = 0, variant = \"rise\", duration = 480, className, ...props }: TextRevealProps) {\n  const host = React.useRef<HTMLElement>(null);\n  const { enabled, inView } = useMotionVisibility(host);\n  const played = React.useRef<string | null>(null);\n  const token = JSON.stringify([text, replayKey]);\n  React.useEffect(() => {\n    if (!inView || !host.current) return;\n    if (played.current === token) return;\n    played.current = token;\n    if (!enabled) return;\n    const words = Array.from(host.current.querySelectorAll<HTMLElement>(\"[data-reveal-word]\"));\n    const milliseconds = Number.isFinite(duration) ? Math.min(1500, Math.max(0, duration)) : 480;\n    if (milliseconds === 0) return;\n    const animations = words.map((word, index) => word.animate([\n      { opacity: 0, transform: variant === \"rise\" ? \"translateY(.32em)\" : \"none\" },\n      { opacity: 1, transform: \"none\" },\n    ], { duration: milliseconds, delay: Math.min(index * 28, 420), easing: \"cubic-bezier(.2,.65,.25,1)\", fill: \"backwards\" }));\n    return () => animations.forEach((animation) => animation.cancel());\n  }, [token, enabled, inView, duration, variant]);\n  return <Tag {...props} ref={host as React.Ref<HTMLHeadingElement>} data-slot=\"text-reveal\" className={cn(\"v-text-reveal\", className)}>\n    <span className=\"v-text-reveal__accessible\">{text}</span>\n    <span aria-hidden=\"true\">{text.split(/(\\s+)/).map((word, index) => /^\\s+$/.test(word) ? word : <span key={index} data-reveal-word=\"\">{word}</span>)}</span>\n  </Tag>;\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/sahajiv/styles/text-reveal.css",
      "content": "@layer sahajiv-states {\n.v-text-reveal { overflow-wrap: anywhere; }\n.v-text-reveal [data-reveal-word] { display: inline-block; }\n.v-text-reveal__accessible { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap; border: 0; }\n\n}\n",
      "type": "registry:file",
      "target": "styles/sahajiv/text-reveal.css"
    }
  ],
  "css": {
    "@import \"@/styles/sahajiv/text-reveal.css\"": {}
  },
  "meta": {
    "source": {
      "name": "Text reveal",
      "variants": [
        "rise",
        "fade"
      ],
      "sizes": [
        "default"
      ],
      "states": [
        "rest",
        "revealing",
        "reduced motion"
      ]
    },
    "api": [
      {
        "name": "TextRevealProps",
        "props": [
          {
            "name": "text",
            "type": "string",
            "required": true,
            "description": ""
          },
          {
            "name": "as",
            "type": "\"span\" | \"p\" | \"h1\" | \"h2\" | \"h3\"",
            "required": false,
            "description": ""
          },
          {
            "name": "replayKey",
            "type": "string | number",
            "required": false,
            "description": "Change this value to deliberately replay the reveal."
          },
          {
            "name": "variant",
            "type": "\"rise\" | \"fade\"",
            "required": false,
            "description": ""
          },
          {
            "name": "duration",
            "type": "number",
            "required": false,
            "description": ""
          }
        ]
      }
    ],
    "category": "Effects",
    "fidelity": "refined-design-family",
    "baseComponent": false
  },
  "type": "registry:ui"
}
