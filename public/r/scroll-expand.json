{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "scroll-expand",
  "title": "Scroll Expand",
  "description": "A framed composition opens as native scroll or controlled progress advances.",
  "dependencies": [],
  "registryDependencies": [
    "https://luv-jeri.github.io/sahajiv-ui/r/sahajiv.json"
  ],
  "files": [
    {
      "path": "registry/sahajiv/ui/scroll-expand.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { useMotionVisibility } from \"@/lib/sahajiv-motion/use-motion-visibility\";\nimport { boundedNumber } from \"@/lib/sahajiv/reference-effect-geometry\";\nimport { cn } from \"@/lib/utils\";\n\nexport type ScrollExpandProps = React.ComponentProps<\"div\"> & {\n  /** Controlled 0–1 progress; omit to follow the document's native scroll. */\n  progress?: number;\n  startScale?: number;\n  paused?: boolean;\n};\nexport function ScrollExpand({ progress, startScale = .65, paused = false, children, className, ref, ...props }: ScrollExpandProps) {\n  const host = React.useRef<HTMLDivElement>(null), frame = React.useRef<HTMLDivElement>(null);\n  const { enabled, inView } = useMotionVisibility(host);\n  const scale = boundedNumber(startScale, .65, .3, 1);\n  React.useEffect(() => {\n    const element = host.current, surface = frame.current;\n    if (!element || !surface) return;\n    let pending = 0;\n    const paint = () => {\n      pending = 0;\n      const box = element.getBoundingClientRect();\n      const p = !enabled ? 1 : boundedNumber(progress ?? (window.innerHeight * .9 - box.top) / Math.max(1, window.innerHeight * .65), 1, 0, 1);\n      surface.style.transform = `scale(${scale + (1 - scale) * p})`;\n      surface.style.borderRadius = `${28 - 12 * p}px`; element.dataset.progress = p.toFixed(3);\n    };\n    const schedule = () => { if (!paused && !pending) pending = requestAnimationFrame(paint); };\n    paint();\n    if (progress === undefined && enabled && inView && !paused) window.addEventListener(\"scroll\", schedule, { passive: true });\n    const observer = new ResizeObserver(schedule); observer.observe(element);\n    return () => { observer.disconnect(); cancelAnimationFrame(pending); window.removeEventListener(\"scroll\", schedule); };\n  }, [enabled, inView, paused, progress, scale]);\n  return <div {...props} ref={React.useCallback((node: HTMLDivElement | null) => { host.current = node; if (typeof ref === \"function\") return ref(node); if (ref) ref.current = node; }, [ref])} data-slot=\"scroll-expand\" data-running={enabled && inView && !paused ? \"true\" : \"false\"} className={cn(\"v-scroll-expand\", className)}><div ref={frame} className=\"v-scroll-expand__surface\">{children}</div></div>;\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/sahajiv/styles/scroll-expand.css",
      "content": "@layer sahajiv-states {\n.v-scroll-expand { width:100%; min-width:0; padding-block:32px; overflow:clip; }\n.v-scroll-expand__surface { width:100%; min-width:0; transform-origin:center; border-radius:16px; background:var(--v-blue); color:var(--v-on-accent); overflow:hidden; }\n@media(prefers-reduced-motion:reduce) { .v-scroll-expand__surface { transform:none; } }\n\n}\n",
      "type": "registry:file",
      "target": "styles/sahajiv/scroll-expand.css"
    }
  ],
  "css": {
    "@import \"@/styles/sahajiv/scroll-expand.css\"": {}
  },
  "meta": {
    "source": {
      "name": "Scroll Expand",
      "variants": [
        "default"
      ],
      "sizes": [
        "default"
      ],
      "states": [
        "rest",
        "active",
        "paused",
        "reduced-motion"
      ],
      "reviewOnly": false
    },
    "api": [
      {
        "name": "ScrollExpandProps",
        "props": [
          {
            "name": "progress",
            "type": "number",
            "required": false,
            "description": "Controlled 0–1 progress; omit to follow the document's native scroll."
          },
          {
            "name": "startScale",
            "type": "number",
            "required": false,
            "description": ""
          },
          {
            "name": "paused",
            "type": "boolean",
            "required": false,
            "description": ""
          }
        ]
      }
    ],
    "category": "Effects",
    "fidelity": "refined-design-family",
    "baseComponent": false
  },
  "type": "registry:ui"
}
