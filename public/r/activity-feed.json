{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "activity-feed",
  "title": "Activity feed",
  "description": "Chronological activity composed from native avatars, badges, content rows and motion surfaces.",
  "dependencies": [],
  "registryDependencies": [
    "https://luv-jeri.github.io/sahajiv-ui/r/sahajiv.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/animated-icon.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/avatar.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/badge.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/button.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/empty.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/item.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/presence.json",
    "https://luv-jeri.github.io/sahajiv-ui/r/typography.json"
  ],
  "files": [
    {
      "path": "registry/sahajiv/ui/activity-feed.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { AnimatedIcon } from \"@/components/ui/animated-icon\";\nimport { Avatar, AvatarFallback, AvatarImage } from \"@/components/ui/avatar\";\nimport { Badge, type BadgeProps } from \"@/components/ui/badge\";\nimport { Button } from \"@/components/ui/button\";\nimport { Empty, EmptyDescription, EmptyTitle } from \"@/components/ui/empty\";\nimport { ItemContent, ItemDescription, ItemTitle } from \"@/components/ui/item\";\nimport { MotionPresence, MotionSurface } from \"@/components/ui/presence\";\nimport { BodySecondary, Meta, Title } from \"@/components/ui/typography\";\n\nexport type ActivityEntry = {\n  id: string;\n  title: React.ReactNode;\n  description?: React.ReactNode;\n  actor?: { name: string; initials?: string; avatarSrc?: string };\n  timestamp?: React.ReactNode;\n  /** Machine-readable time matching the caller's timestamp label. */\n  dateTime?: string;\n  badge?: { label: React.ReactNode; variant?: BadgeProps[\"variant\"] };\n  /** Additional supplied details or native actions, kept in document flow. */\n  content?: React.ReactNode;\n};\nexport type ActivityFeedProps = Omit<React.ComponentProps<\"section\">, \"children\" | \"title\"> & {\n  /** Caller supplies the chronological order; the feed never reorders entries. */\n  entries: readonly ActivityEntry[];\n  title?: React.ReactNode;\n  description?: React.ReactNode;\n  empty?: React.ReactNode;\n  /** Omit to show every supplied entry, including later appends. */\n  initialVisible?: number;\n  pageSize?: number;\n  showMoreLabel?: React.ReactNode;\n};\n\nfunction count(value: number | undefined, fallback: number) {\n  return value !== undefined && Number.isFinite(value) ? Math.max(1, Math.floor(value)) : fallback;\n}\n\nexport function ActivityFeed({\n  entries, title, description, empty, initialVisible, pageSize = 3, showMoreLabel,\n  className, \"aria-label\": ariaLabel, \"aria-labelledby\": labelledBy, ...props\n}: ActivityFeedProps) {\n  const titleId = React.useId();\n  const listId = React.useId();\n  const list = React.useRef<HTMLOListElement>(null);\n  const focusIndex = React.useRef<number | null>(null);\n  const [limit, setLimit] = React.useState(() => count(initialVisible, Infinity));\n  const [announcement, setAnnouncement] = React.useState(\"\");\n  const visible = entries.slice(0, limit);\n  const remaining = entries.length - visible.length;\n  const nextPage = Math.min(remaining, count(pageSize, 3));\n  React.useLayoutEffect(() => {\n    if (focusIndex.current === null) return;\n    list.current?.querySelectorAll<HTMLElement>(\"[data-activity-entry]\")[focusIndex.current]?.focus();\n    focusIndex.current = null;\n  }, [visible.length]);\n  const reveal = () => {\n    focusIndex.current = visible.length;\n    const nextCount = visible.length + nextPage;\n    setLimit(nextCount);\n    setAnnouncement(`${nextCount} of ${entries.length} activities shown.`);\n  };\n  return <section {...props} data-slot=\"activity-feed\"\n    aria-label={ariaLabel ?? (!title && !labelledBy ? \"Activity\" : undefined)}\n    aria-labelledby={labelledBy ?? (title ? titleId : undefined)}\n    className={cn(\"v-activity-feed\", className)}>\n    {(title || description) && <header className=\"v-activity-feed__header\">\n      {title && <div className=\"v-activity-feed__heading\"><Title id={titleId}>{title}</Title><Badge variant=\"count\" aria-label={`${entries.length} activities`}>{entries.length}</Badge></div>}\n      {description && <BodySecondary as=\"div\">{description}</BodySecondary>}\n    </header>}\n    {entries.length === 0 ? empty ?? <Empty>\n      <EmptyTitle>No activity yet</EmptyTitle>\n      <EmptyDescription>Updates appear here as they are added.</EmptyDescription>\n    </Empty> : <>\n      <ol ref={list} id={listId} className=\"v-activity-feed__list\" role=\"list\">\n        <MotionPresence>\n          {visible.map(entry => <MotionSurface key={entry.id} asChild preset=\"rise\">\n            <li tabIndex={-1} data-activity-entry={entry.id} className=\"v-activity-feed__entry\">\n              <div className=\"v-activity-feed__portrait\" aria-hidden=\"true\">\n                {entry.actor ? <Avatar variant=\"sm\">\n                  {entry.actor.avatarSrc && <AvatarImage src={entry.actor.avatarSrc} alt=\"\" />}\n                  <AvatarFallback>{entry.actor.initials ?? entry.actor.name.trim().split(/\\s+/).slice(0, 2).map(part => Array.from(part)[0]).join(\"\")}</AvatarFallback>\n                </Avatar> : <span className=\"v-activity-feed__marker\"><AnimatedIcon name=\"sparkles\" size=\"sm\" preset=\"draw\" /></span>}\n              </div>\n              <ItemContent className=\"v-activity-feed__content\">\n                <div className=\"v-activity-feed__entry-heading\">\n                  <ItemTitle className=\"v-activity-feed__title\">{entry.title}</ItemTitle>\n                  {entry.badge && <Badge size=\"sm\" variant={entry.badge.variant ?? \"cream\"} className=\"v-activity-feed__badge\">{entry.badge.label}</Badge>}\n                </div>\n                {entry.description && <ItemDescription className=\"v-activity-feed__description\">{entry.description}</ItemDescription>}\n                {(entry.actor || entry.timestamp) && <div className=\"v-activity-feed__meta\">\n                  {entry.actor && <Meta>{entry.actor.name}</Meta>}\n                  {entry.actor && entry.timestamp && <span aria-hidden=\"true\">·</span>}\n                  {entry.timestamp && <time dateTime={entry.dateTime}><Meta>{entry.timestamp}</Meta></time>}\n                </div>}\n                {entry.content && <div className=\"v-activity-feed__details\">{entry.content}</div>}\n              </ItemContent>\n            </li>\n          </MotionSurface>)}\n        </MotionPresence>\n      </ol>\n      {remaining > 0 && <footer className=\"v-activity-feed__footer\">\n        <Button variant=\"secondary\" size=\"sm\" aria-controls={listId} onClick={reveal}>\n          <AnimatedIcon name=\"chevron-down\" size=\"sm\" preset=\"bounce\" />\n          {showMoreLabel ?? `Show ${nextPage} more`}\n        </Button>\n        <Meta>{visible.length} of {entries.length}</Meta>\n      </footer>}\n    </>}\n    <span role=\"status\" aria-live=\"polite\" className=\"sr-only\">{announcement}</span>\n  </section>;\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/sahajiv/styles/activity-feed.css",
      "content": "@layer sahajiv-states {\n.v-activity-feed[data-slot] { min-width:0; width:100%; color:var(--v-text); }\n.v-activity-feed__header { display:grid; gap:8px; margin-block-end:18px; }\n.v-activity-feed__header :is(h3,p) { margin:0; }\n.v-activity-feed__heading { display:flex; align-items:center; flex-wrap:wrap; gap:12px; }\n.v-activity-feed__list { display:grid; list-style:none; margin:0; padding:0; }\n.v-activity-feed__entry { position:relative; display:grid; grid-template-columns:32px minmax(0,1fr); gap:16px; min-width:0; padding-block:20px; outline-offset:5px; }\n.v-activity-feed__entry::marker { content:none; }\n.v-activity-feed__entry + .v-activity-feed__entry { border-block-start:1px solid var(--v-edge); }\n.v-activity-feed__entry:focus-visible { outline:2px solid var(--v-blue); }\n.v-activity-feed__portrait { display:flex; justify-content:center; padding-block-start:2px; }\n.v-activity-feed__marker { display:grid; place-items:center; inline-size:28px; block-size:28px; border-radius:43% 57% 50% 50% / 55% 44% 56% 45%; background:var(--v-beige); color:var(--v-text); }\n.v-activity-feed__content[data-slot] { gap:7px; min-width:0; }\n.v-activity-feed__entry-heading { display:flex; align-items:baseline; flex-wrap:wrap; gap:7px 12px; min-width:0; }\n.v-activity-feed__title[data-slot] { display:block; overflow:visible; -webkit-line-clamp:unset; font-size:15.5px; line-height:1.5; overflow-wrap:anywhere; }\n.v-activity-feed__badge[data-slot] { flex-shrink:0; max-width:100%; min-height:20px; height:auto; padding-block:3px; white-space:normal; line-height:1.2; }\n.v-activity-feed__description[data-slot] { font-size:14px; line-height:1.55; max-width:68ch; overflow-wrap:anywhere; }\n.v-activity-feed__description :first-child { margin-block-start:0; }\n.v-activity-feed__description :last-child { margin-block-end:0; }\n.v-activity-feed__meta { display:flex; align-items:center; flex-wrap:wrap; gap:6px; color:var(--v-text-2); min-width:0; }\n.v-activity-feed__meta :is(time,span) { overflow-wrap:anywhere; }\n.v-activity-feed__details { display:grid; gap:8px; margin-block-start:5px; min-width:0; overflow-wrap:anywhere; }\n.v-activity-feed__footer { display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:12px; padding-block-start:16px; border-block-start:1px solid var(--v-edge); }\n@media(max-width:480px) { .v-activity-feed__entry { column-gap:12px; padding-block:18px; } .v-activity-feed__footer { justify-content:flex-start; gap:12px 20px; } }\n\n}\n",
      "type": "registry:file",
      "target": "styles/sahajiv/activity-feed.css"
    }
  ],
  "css": {
    "@import \"@/styles/sahajiv/activity-feed.css\"": {}
  },
  "meta": {
    "source": {
      "name": "Activity feed",
      "variants": [
        "default"
      ],
      "sizes": [
        "default"
      ],
      "states": [
        "rest",
        "appended",
        "expanded",
        "empty",
        "reduced-motion"
      ]
    },
    "api": [
      {
        "name": "ActivityFeedProps",
        "props": [
          {
            "name": "entries",
            "type": "readonly ActivityEntry[]",
            "required": true,
            "description": "Caller supplies the chronological order; the feed never reorders entries."
          },
          {
            "name": "title",
            "type": "React.ReactNode",
            "required": false,
            "description": ""
          },
          {
            "name": "description",
            "type": "React.ReactNode",
            "required": false,
            "description": ""
          },
          {
            "name": "empty",
            "type": "React.ReactNode",
            "required": false,
            "description": ""
          },
          {
            "name": "initialVisible",
            "type": "number",
            "required": false,
            "description": "Omit to show every supplied entry, including later appends."
          },
          {
            "name": "pageSize",
            "type": "number",
            "required": false,
            "description": ""
          },
          {
            "name": "showMoreLabel",
            "type": "React.ReactNode",
            "required": false,
            "description": ""
          }
        ]
      }
    ],
    "category": "Data display",
    "fidelity": "refined-design-family",
    "baseComponent": false
  },
  "type": "registry:ui"
}
