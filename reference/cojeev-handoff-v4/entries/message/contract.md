# Message — contract

- **id** `message` · **tier** base · **evidence** I09 I16 · **status** source
- **root** `.v-msg`
- **api** .v-msg[.-me] > .v-disk (avatar) + __stack (.v-bubble…, .v-prov) · provenance line "Summary generated by Cojeev"
- **isolation canvas** 480x240 · **files** `isolation/message/`

## Modifiers
| class | meaning |
|---|---|
| `.-me` | variant: me — the person (right-aligned) |

## Sizes
one size (`default`)

## States (how each is expressed)
| state | expression |
|---|---|
| `rest` | default |

## Parts (class → data-part; classes are never renamed)
| selector | part |
|---|---|
| `.v-msg` | `data-part="root"` |
| `.v-msg__stack` | `data-part="content"` |
| `.v-prov` | `data-part="description"` |

## Keyboard
none (static)

## Accessibility
- Every icon-only control carries `aria-label`; every input has a label (`<label for>`, wrapping `<label>` or `aria-label`).
- Real `<button>`, `<a>`, `<input>`; text never truncates.
- State is announced through aria (`aria-pressed`, `aria-selected`, `aria-current`, `aria-expanded`, `aria-busy`, `aria-invalid`); `data-state` mirrors it for styling and testing and is never the source of truth.
- Focus: 2 px ring `var(--ring)` on `:focus-visible`; overlays trap focus, close on Escape and return focus to the opener.

## Motion
- **category** surfaces · **on by default** no (motion.js) · **travelling selection / flow roles** enters
- Per-element override: `data-morph` on, `data-motion="off"` off. Full contract in `docs/MOTION.md`, rows in `data/motion.json`.

## Classes seen in the catalog demo
v-bubble, v-disk, v-msg, v-msg__stack, v-prov, v-shape
