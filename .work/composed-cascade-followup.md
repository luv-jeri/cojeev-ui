# Source cascade and Radix field-state follow-up

At the fixed follow-up revision, Pagination, DataTable, NavigationMenu, and Kbd default/light/360px each pass with zero computed-style differences and zero comparator pixels. Evidence: artifacts/gate-composed-cascade-followup/results.json. The parent transparent-fill correction a90c918 restores Pagination paint; the info layout change remains source exact.

Source cascade evidence: reference/sahajiv-handoff-v4/js/morph.js:373 injects [data-morph]{transition:transform 0s} after flow. Source flow.css:208 has the one-class Kbd timing selector; flow.css:211 has the stronger two-class table-Disk timing selector. The port equivalents are registry/sahajiv/styles/flow-press.css:129 and :132. Previously morph.css sat in a later cascade layer and always won. The transition declaration now sits at flow-press.css:169 after the flow rules, with specificity one via [data-morph]:where([data-slot].v-morph-host). Strong table/group contexts now win and equal-specificity Kbd retains the source later declaration. The global layer order is unchanged.

Nested Radix NavigationMenuLink receives source direct-child transition, hover and stacking behavior; nav-specific active pink and the rgba(251,244,230,.1) outer well restore the complete source CSS paint. Segmented controls accept Radix aria-checked for transparent selected hosts. Fill groups now prefer an explicit activeSelector, then actual focus, allowing the real unified-input OTP state to drive its visual cells. The source OTP inset-shadow exception includes the equivalent owned slot. A browser probe against the production attachFlowGroup verifies explicit initial state 1, explicit precedence over focus on 2, real-focus fallback to 2 after clearing the state, controlled state change back to 1, no inset shadow on the OTP cell, and disposal cleanup. This probes the shared engine seam; the interactive owner validates actual InputOTP integration.

Focused registry/fixture typecheck, flow ESLint, CSS parse with no important declarations, and diff whitespace checks pass. Full composed isolation matrix follows on a frozen revision. The full-cascade blob-effects discrepancy remains OPEN: prior engine-required-style parity is separate evidence, and complete vriksha.css effect parity has not been established. No source files were modified and no reference design refinements were introduced.

Correction after live source inspection by the interactive owner: the source after its full boot uses vf-enter, not the intermediate v-acc-in declaration in components-2.css:527. The 9b12add removal was superseded by the integrated interactive correction, which restores vf-enter and preserves duration-zero fill behavior under reduced motion. The earlier stylesheet-order hypothesis was not a verified source result. The independently measured table/Kbd transition findings above remain valid.
