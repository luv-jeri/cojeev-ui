# Composed field and motion checkpoint

Changes: FieldSet and FieldGroup register their own field-travel groups. FieldGroup accepts asChild for an existing layout container. The fixture identifies the source field grouping boundaries in its detached document and maps those containers to FieldGroup; production performs no global document scan. Shared flow preserves the native-label root timing for Radix Checkbox/Radio, applies indicator easing, keeps easing under reduced motion with duration zero, and aliases Accordion chevrons. Breadcrumb direct icons use the authored 14px context. Pagination info uses the exact authored clipped-box geometry rather than Tailwind sr-only negative margin.

Latest bounded 360px/light/default checks: attachment, breadcrumb, button-group, carousel, chart, field, input, input-group, questionnaire, sidebar passed. Input-group pixel difference was 0.0108%, within the existing gate comparator limit; the others in their latest run had zero computed-style and pixel differences. Earlier unchanged message-scroller, native-select, stepper, table, textarea checks passed. Pagination now has zero computed differences but 0.3160% pixels due to the centrally confirmed transparent morph-fill regression; parent owns the fix. DataTable has four Disk easing differences and 0.3188% pixels. Dropzone retains the documented source isolation loader gap: its page omits alive.js, so source Spinner is empty. These are diagnostics, not complete all-width/all-variant/dark approval.

Actual live Pagination travel: source and candidate glide/jelly/rubber passed native selection movement with no sustained port frame-gap regression. Source glide had one 83.3ms gap/86ms long task; candidate glide and jelly each had one 33.3ms gap; other maxima were 16.8ms. All p95 values were 16.7–16.8ms, selection ended on 8, pill present, no page errors.

Actual docs Button versus source catalog Button: Chromium 1440x1000, fresh independent loads, fonts plus 1800ms settle, real pointer movement and press. Source and docs both p95 16.7ms, max 16.8ms, zero >32ms gaps and zero long tasks. Both show changing morph paths and reversible press transforms. Docs workload differs: 58 total/10 visible morph bodies, 8 total/3 visible flow groups, 63 CSS animations; source 37/23 morph bodies, 59/2 flow groups, 17 CSS animations. Measured script time 132ms docs versus 83ms source; style recalculation 87ms versus 24ms. This bounded run did not reproduce reported docs choppiness and does not establish all-page smoothness. Target hover screenshots were visually inspected; native press and settling states were recorded. No animation redesign was made.

Artifacts: artifacts/gate-composed-final/docs-live/results.json; artifacts/gate-composed-final/live-pointer-frame-gaps.json; artifacts/gate-composed-context-final/results.json; artifacts/gate-composed-questionnaire-final/results.json. These generated artifacts remain local. Focused registry/fixture typecheck, changed TSX ESLint, CSS parsing and diff whitespace checks are recorded at commit time.
