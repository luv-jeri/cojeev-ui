# Final bounded interactive follow-up

The final requested initial-state follow-up used18d000d (including a6c7ceb and automatic radius capture d8bdae2) with three independent ports. Calendar24/24 and ToggleGroup-on12/12 are exact across360/390/768/1024/1440/1920, both themes. Their committed receipts supersede earlier WK-header and responsive-radius failures.

The initial selection36-row receipt is retained: it revealed the intentional Radix Select window-resize dismissal. Roota077a48 changes only that scenario to prepare viewport before opening, on both sides. Final Select12-row replacement results are recorded separately with `statePreparation.viewport=before-open`; no raw source or production Select behavior was altered to keep a dismissed menu mounted.

The owned popup defaults checkpoint44146c1 was then merged with roota077a48 into d12fca0. HoverCard12-row full-scene follow-up remains FAIL and stable on both sides in every row. Exactly21 sampled differences persist per row:18 excluded health product-child properties; the same derived21.453px content increase sampled on wrapper and inner card; and source CSS left/top versus actual Radix positioning transform. Missing visible wrapper identity is fixed.

Source ui.js180 only computes HoverCard position when show runs. The six-width protocol opens at1920 then changes widths; at360/390 its unchanged left position clips the source card offscreen, while Radix updates to fit. This is a separate source resize behavior difference, plainly visible in the preserved full-scene screenshots. It must not be silently attributed to health-line. The direct360 capture already records the smaller0.68% residual without that resize effect.

Read-only screenshot inspection also identifies the anchor underline missing in candidate: source base.css10 leaves native a[href] underline intact; Tailwind reset removes it. This is outside the health subtree. Restored only text-decoration-line:underline on owned a[href][data-slot=hover-card-trigger]; no global anchor styles changed. This final one-property baseline correction is source-inspected and is not included in the already completed frozen browser receipts. No additional suite was run.

The strict scope classification is in .work/interactive-health-line-scope.md. Original full scenes remain unaccepted. No further Calendar/ToggleGroup or924-row matrix was repeated.

Final Select12 rows all remain FAIL with exactly six source ancestor width/height differences per row. The list and all three options remain visible; trigger/option pointer-outline differences are absent. Both sides were stable in every row; no runtime errors. The retained failure is the previously documented normal-flow source menu expansion versus the real portaled popup.
