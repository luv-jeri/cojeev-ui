# Historical reference issues and refinement follow-up

The original baseline prioritised exact matching. The owner has since authorized production refinements while preserving the visual family. This file records historical findings; original failing receipts remain unchanged. Intentional improvements are tracked in REFINEMENTS.md.

- **Dark Card text:** the original dark rule for `.v-card .v-meta`, `.v-body-2` and `.v-caps` overrides the ink used on physical pink/yellow/olive/blue surfaces. Keep the authored baseline; review contrast in the next improvement stage.
- **Bubble timestamp:** the original timestamp uses `--structure-text` while its container uses `--v-ink`, which changes in dark mode. Keep the original baseline and revisit the mismatched surface/foreground pair later.
- **Morph effects under the complete component cascade:** `components-2.css` rules at lines 460 and 483 override fills on echo, sheen, grain and color paths. The engine-only comparison and full-cascade comparison must be reported separately. The baseline gate must retain this difference until resolved against the actual reference.
- **Frozen flow cancellation:** the original frozen clock returns an object handle which a cancellation branch passes to native `clearTimeout`. The motion gate documents its narrow clock adapter and records the literal-source probe; normal runtime behavior is the target.
- **Missing loader runtime in isolation and entry demo pages:** the full catalog loads original `js/alive.js` (catalog/index.html:202); generated isolation and entry pages omit it. Pages containing `.v-pulse` are served with that original runtime restored, and the gate records the bootstrap adapter. Reference files remain unchanged. The earlier untouched-loader FAIL screenshots are retained in the local development archive as the `gate-smoke` and `gate-static-entry-demos` runs; the completed Spinner entry bootstrap passes all 12 width/theme comparisons.
- **OTP isolation container:** the generated page drops the catalog's definite grid track and inline-size containment (`catalog/index.html:114,160,172`). At 360px that makes native input intrinsic sizing expand the isolated group to roughly 1,450px; the original catalog fits six cells in a 288px group. The gate restores the surrounding container geometry on both sides and records `otp-catalog-sizing`. It changes no component styles. The unadapted diagnostic remains in the local development archive as the `interactive-base360` run.

The documentation Preview's local muted-text reset addresses context introduced by the documentation helper. It does not change a component's authored standalone colors.

Confirmed catalog runtime defects and the historical decision boundary are detailed in [REFERENCE-RUNTIME-FINDINGS.md](REFERENCE-RUNTIME-FINDINGS.md). Native touch geometry has been corrected. Historical hidden echo paint and synthetic fixture label differences remain recorded in the local development archive under `native-touch-motion-report`. The literal-source hover clock admission probe is archived under `motion-travel-108`; original unstable rows remain alongside their bounded unchanged rechecks. These archived receipts are not part of the public source snapshot; current intentional production changes are recorded in REFINEMENTS.md.
