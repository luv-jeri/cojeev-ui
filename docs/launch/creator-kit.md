# 000h by Cojeev — creator kit

Prepared copy and demo routes. The launch domain is pending; replace `LAUNCH_URL` with the verified HTTPS origin before sharing. No outreach has been sent.

## One sentence

000h by Cojeev is an open-source React component library with organic shapes, purposeful motion, and editable source you can bring into your own project.

## Short introduction

I’m Sanjay, the designer and developer behind 000h by Cojeev. I’m exploring how interfaces can feel a little warmer and more alive while remaining useful. Each component has a working demo, source code and an installation command, so you can try the details and make them your own.

## Useful links

| Purpose | Route |
| --- | --- |
| Overview and live previews | `LAUNCH_URL/` |
| First component | `LAUNCH_URL/getting-started/` |
| Organic wordmark | `LAUNCH_URL/docs/semantic-bloom/` |
| Layered drawer | `LAUNCH_URL/docs/motion-drawer/` |
| Animated details | `LAUNCH_URL/docs/animated-icon/` |
| SVG shape workbench | `LAUNCH_URL/docs/shape-artwork/` |
| About the maker | `LAUNCH_URL/about/` |
| Source and license | https://github.com/luv-jeri/cojeev-ui |

## A personal first message

Reference one real detail in the creator's recent work, then introduce the relevant component:

> I made 000h by Cojeev, an open-source React component library, and thought this particular demo might fit what you’re building. You can try it here and copy the source into your project. If you give it a look, I’d love to know which part you’d actually use.

Replace “this particular demo” with the component and why it fits. Link directly to that demo. Do not claim a creator has tried or endorsed it, and do not send repeated generic comments.

## Attribution links

Use lowercase campaign slugs without personal data:

`LAUNCH_URL/docs/semantic-bloom/?utm_source=CREATOR-SLUG&utm_medium=creator_outreach&utm_campaign=directory_launch&utm_content=semantic-bloom`

The directory itself adds its own campaign parameters. Its links may hide the browser referrer, so keep campaign attribution independent of the referrer. Analytics measures anonymous activity within a document lifetime; a full reload starts a new identity. Do not call this a count of unique people.

## Recording checklist

Record the deployed build after installation and visual checks pass. Show one meaningful interaction, a visible component name, and the copy-to-project path. Use each component's actual controls, keep text readable on a phone, and include a quieter-motion version when relevant. The generated social image is a link preview; it is not a demo recording.
