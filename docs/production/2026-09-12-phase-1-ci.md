# B01 — CI catalogue partitioning

Date: 12 September 2026. Checkpoint B01; implementation and local characterization complete, full remote acceptance pending.

## Observed failure

[Run 34666264387](https://github.com/luv-jeri/cojeev-ui/actions/runs/34666264387), source `cc971887e4e6825bd5568815cb76ebf22c602e04`, was cancelled. Its [Verify release job](https://github.com/luv-jeri/cojeev-ui/actions/runs/34666264387/job/103478640491) started at 01:56:41 UTC and completed at 03:27:07 UTC. The configured execution budget is 90 minutes; cancellation and final evidence upload account for the final cleanup interval.

The catalogue/motion gate step ran from 02:03:39.734 to 03:26:54.450 UTC in the captured log, approximately 83 minutes 15 seconds, before cancellation. GitHub's job API independently confirms the corresponding whole-second timestamps and cancelled conclusion. Every preceding verification step passed, including unit/reporting/hosting tests, both release environment builds, CSP, the Pages fixture build and example compilation. The checklist records 132 of 172 catalogue entries reached without recorded failures before cancellation; this is incomplete coverage, not a passing gate.

All subsequent mobile, marketing, smooth-scroll, reporting-browser, analytics and clean-consumer release checks were skipped. Sanitized browser evidence upload succeeded during cleanup. This failure is timing evidence for the serial configuration; local synthetic timings cannot establish a replacement CI budget.

Read-only diagnosis commands:

```sh
gh api repos/luv-jeri/cojeev-ui/actions/runs/34666264387 --jq '{head_sha,conclusion,html_url,run_started_at}'
gh api repos/luv-jeri/cojeev-ui/actions/jobs/103478640491 --jq '{name,conclusion,started_at,completed_at,steps:[.steps[]|{name,conclusion,started_at,completed_at}]}'
```

## Scoped change

The `npm run gate` workflow step now sets `COJEEV_DOCS_SHARDS: '3'`. The existing production runner already accepts one to three workers, distributes component IDs by position modulo worker count, starts the documentation children concurrently and aggregates their reports before motion verification. Each real documentation child requests its own static preview server on an ephemeral loopback port.

The 90-minute job limit, all subsequent release checks, failure behavior, exact-once coverage, fresh-evidence checks, source-provenance assertions, release artifacts, deployment jobs and production approval gate are unchanged. No production runner architecture or application code changed.

## Executable characterization and local results

The new `tests/production-gate.test.mjs` executes an unchanged copy of the actual `scripts/run-production-gate.mjs` in a disposable directory. Only its documentation and motion child-browser programs are replaced by `tests/fixtures/production-gate-child.mjs`. The actual process spawning, waiting, aggregation, assertions, evidence formatting and exit-code propagation execute normally. No source-text matching is used to assert runner behavior.

Fourteen cases cover:

- One, two and three workers: exact ID partitions, unique child processes, output directories, source-snapshot argument forwarding, aggregated provenance and chrome checks, evidence-directory mapping, registry digest and motion invocation.
- A child startup barrier: all workers must start before any produces a report, so a serial implementation fails without relying on a speed threshold.
- Documentation and motion nonzero exits propagate to gate failure; a documentation failure with complete reports still runs the later motion check.
- Missing/stale reports, duplicate/missing component IDs, differing worker snapshots and source changes all fail before aggregate evidence is published.
- Unsupported counts (zero, four and a fraction) fail before child execution.

Executed with Node 22.22.0 on branch `codex/fix/b01-ci-partitions`, based on `cc971887e4e6825bd5568815cb76ebf22c602e04`:

| Command | Result |
| --- | --- |
| `node --test tests/production-gate.test.mjs` | 14 passed, 0 failed/skipped/cancelled; 1.426 seconds |
| `npm test` | 343 passed, 0 failed/skipped/cancelled; 11.351 seconds |
| `node node_modules/eslint/bin/eslint.js tests/production-gate.test.mjs tests/fixtures/production-gate-child.mjs --max-warnings=0` | Passed, exit 0 |
| `git diff --check` | Passed |

These are characterization tests of existing runner behavior; they passed on their first execution. No new runner defect or red/green correction is claimed. The old cancelled remote run is the observed failure motivating the configuration change. The fixture exercises orchestration, not browser rendering or actual preview-server isolation.

## Remote acceptance and remaining gate

**Pending:** the complete required workflow on the final reviewed candidate. No full local browser gate, new remote workflow, PR, merge or deployment was performed for this implementation handoff. B01 stays open until the complete remote job, including every later release check, succeeds inside the unchanged budget. Record the exact candidate SHA, run URL, job duration and conclusions here after that run finishes.

Three concurrent Chromium workers increase peak CPU/memory use, and catalogue partitions can have uneven duration. The full remote run must establish whether the existing bounded concurrency meets the budget on the GitHub runner. Rollback is removal of the step's two-line environment setting; this restores serial execution and its known timing limitation without changing the runner or release protections.
