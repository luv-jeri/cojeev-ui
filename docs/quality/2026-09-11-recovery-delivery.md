# Recovery delivery — 11 September 2026

**Later release verification:** The remaining local release checks are now complete: production build, 293 unit tests, full lint, 791 copied snippets, clean consumer installation/runtime, and the 172-entry / 1,032-layout catalogue pass with isolated rechecks. Additional chart, tablet-overflow and interrupted quiet-exit fixes are delivered. See [final verification and limits](2026-09-11-release-verification.md). The earlier runtime paths and limited gate counts below are historical checkpoints; the current preview uses `/private/tmp/cojeev-release-71N1IP` on port 4321. No public deployment or owner aesthetic approval is implied.

Recovery checkpoint: **55 of 55 intake rows, the separate twelve-concept Hero Button collection, and the scoped shared-shell requests have implementation and local verification evidence.** The later Bento request supersedes the original AspectRatio redesign; its primitive and old route remain compatible. The primary assistant performed the recovery and representative visual inspection without sub-agents. This recovery checkpoint did not assert owner aesthetic acceptance, a production build, deployment, or complete behavioral certification of all 172 catalogue components. The later release report above supersedes its technical gate counts; earlier counts below remain historical checkpoints.

## Final integration — Shape Studio, Bento and the shared shell

Shape and ShapeArtwork now use one editor, also mounted on the landing page: one proofing stage, twelve signature contours, readable colour/layer controls, Still/Breathe, retained configuration and SVG/React export. All 36 legacy masks and specialized 3D/effect components remain available rather than being deleted as supposed duplicates. Still SVG export is labelled honestly; copied React includes motion and validated timing. Both documentation examples include the complete shared editor source. Actual docs/landing tests, five export/geometry tests, eight responsive/theme captures and personal desktop/mobile/detail review pass. The final spacing adjustment separates the layer controls without adding another competing illustration.

Bento now provides four templates, Classic and complementary Interlock edges, editable rows/columns/labels, content swapping, snapped seam resizing, Undo/Redo and visible/live feedback for blocked movements. AspectRatio points composition users to Bento while preserving its low-level API. Native and actual-docs checks cover primary-pointer capture, cancellation, drag-time geometry, commit/Undo, blocked/recovered movement, keyboard alternatives, copied layout and responsive themes.

### The flicker in the owner's 11:57 recording

The recording exposed a real layout feedback loop, not merely an opacity transition. With the pointer almost stationary, content-sized fractional grid rows made the board alternate between approximately 410px and 654px. The same pointer coordinate then selected alternating two-row/three-row sizes. The grip also used the committed tile instead of the visible preview and stayed behind until release.

The editing canvas now has stable 144px row tracks and size-contained, readable tile contents; the reusable BentoGrid remains fluid. Grips follow the live painted tile while their captured native button stays mounted. `tests/bento-resize-stability.docs.browser.mjs` failed on the alternating measured heights, then passed for both Classic and Interlock: fourteen near-stationary pointer samples, unchanged canvas size, one three-row result, grip-to-edge agreement, committed release and Undo. The broader native and docs Bento suites also pass. Final mobile and held-drag compositions were personally inspected. Evidence: `final-integration/bento-resize-stability/frames.json` and its held-state captures, plus `bento-recovery/` and `bento/`.

The docs home is now a four-step annotated setup workbench with exact install commands and Icon, Shape and Bento entry points. Request Board is a restrained workshop ledger with status/demand hierarchy, a small authored shape and truthful unavailable/loading/empty/error states. Search aborts stale requests and pagination deduplicates results. Reporting retains independent drafts and review state, uses shared icons and owned text/source scrolling, and keeps sending disabled when disconnected. Its long-text accessible label is now explicitly bound so the scrollbar value cannot become part of the field name. No real request was submitted by verification.

The theme-icon seam fix preserves the actual backdrop for translucent controls instead of briefly painting an approximation behind the glyph. Fresh light/dark 1x/2x boundary-pixel checks pass, as do native focus and quiet motion. Fresh sidebar compact-index, hover-hit stability, reporting-stack, request-board, workbench and Accordion checks pass. Earlier in this same recovery, the actual Fumadocs search, calm navigation/hover previews, shared Button contract and preview-background gates passed. These checks preserve the craft standard: authored structure, readable hierarchy, one moving paint owner, stable native targets and retained choices—not a count of colour variants.

### Final evidence and limits

- Full source TypeScript and focused lint pass. The final focused unit batch has 33 passes. Each preceding family records its own additional tests below.
- Copied-source validation freshly passes **65 defaults / 374 configured snippets**, with zero extraction, coverage or TypeScript diagnostics. This covers all intake rows and the additional Hero/Bento/shared examples; it is not a rerun of every catalogue snippet.
- The full registry builds 173 payloads. **79 affected/dependency payloads** were source-verified and delivered to the original checkout, preserving unrelated entries. All 65 requested-root dependency/import-closure checks pass against the original source.
- The public-documentation scroll audit visits all 172 routes at 390px and records 670 rendered overflow observations. The only unmanaged observed owner is the intentionally native Select list. This is a rendered default/gallery scan, not every hidden portal state. Separate native checks cover migrated Dock RTL/focus, Resizable panels, ScrollReveal and reporting textarea/source behavior. See [scrollbar scope and exceptions](global-scrollbar-audit.md).
- Final captures and machine-readable receipts are preserved under `output/playwright/recovery-20260911-visual-revision/final-integration/`. Earlier before/after evidence is retained separately. Runtime: exact-lockfile snapshot `/private/tmp/cojeev-recovery-m9KWHR`, port 4321. Original dependencies were not changed.

No commit, push, publication, production-build claim, or owner aesthetic approval is implied. Technical recovery is complete within the recorded intake; the owner's visual judgment remains distinct from my personal review.

## Focused dialogs and the icon typecase

Dialog now has Confirmation, Editor and Exhibit, with retained local note state, honest cancellation, native focus containment/return and shared44px close/actions. Linear Modal now follows the upstream UI Layout card/image/title continuity: Card, Centered and a real browsable Gallery, with legacy Compact preserved. The original implementation was measured fading at a constant720px; the repaired surface and image interpolate forward and in reverse. Native tests cover repeated interruption/reopen, controlled refusal, cancelled Escape, refs, long owned scrolling and quiet/off motion. Personal visual review caught a240px inherited scrollport crop that the initial resting tests missed; explicit viewport/title/action checks now protect it. Upstream source at88d827d7ec342917ca06f6894e5add65fabbe5d8 and its live open/closed reference were inspected; MIT attribution is retained. No claim of quantitatively sampling the upstream transition.

Icon/AnimatedIcon now share a legible typecase: selected proofing glyph, readable controls and a bounded24-tile searchable tray. Outline colour visibly follows the ink picker. Copied plain Icon code uses feedbackDuration/feedbackEase rather than unsupported duration/ease; AnimatedIcon retains its own timing props. All1,722 names and actual geometry remain. Fresh native replay/lifecycle tests, ten geometry/inventory units and existing icon-library interaction tests pass. New actual-docs checks cover colour, timing/copy, treatments, search/empty/clear focus and paging, with24 responsive/theme captures. Dialog/Linear produced48 captures plus a separate state-preservation/visible-content gate. Representative final compositions were personally inspected and corrected.

Full TypeScript, focused lint, four defaults/27 copied snippets with zero diagnostics and173-item registry build passed. Nine affected source-verified payloads were delivered; original Dialog/LinearModal/Icon/AnimatedIcon closures pass. Evidence: `output/playwright/recovery-20260911-visual-revision/{dialog-recovery,icon-studio-recovery}/` and `dialog-icon-copy.json`. Two intake rows and shared-shell acceptance remain. No deployment or owner aesthetic acceptance is implied.

## Deliberate actions and a calm navigator — Toggle, Item and Sidebar

Toggle now exposes Tool, Bookmark and Preference, retaining Pin note state and all original treatments. Held native targets no longer shrink; one shared contour paints the selected state. Toggle-only styling is scoped away from ToggleGroup. Item provides Ledger, Cover and Detail with authored artwork, retained selection and independent Keep actions; detail actions have a non-button outer item. Personal inspection corrected small-screen contour overflow, preference text alignment and a misleading Read action label.

Sidebar offers Folio, readable Index and MotionDrawer compositions with retained destination/draft. Collapse is a stationary44px shared button. Long drawer content now binds the existing native scrollport to the organic scrollbar; both drawer close buttons use shared Button/Icon. Default docs-shell styling remains opt-in isolated. Personal review corrected the gradient being covered by the contour and the collapsed header. Current native tests cover refs, Slot, controlled refusal, cancelled events, disabled, actual changing contour and reduced motion, owned wheel scrolling and focus return. Actual docs produced24 action and14 navigation captures; representative final treatments were personally inspected.

Fresh passes also include the complete MotionDrawer API and Stack suites, documentation-sidebar categories/hover previews/default-open/mobile focus/real navigation suite, full TypeScript and focused lint, four defaults/16 copied snippets with zero diagnostics, and173-item registry build. Ten affected source-verified payloads were delivered. Original Toggle/Item/Sidebar/MotionDrawer/ToggleGroup/Button dependency closures match current source. Evidence: `output/playwright/recovery-20260911-visual-revision/{collection-actions-recovery,sidebar-recovery}/` and `collection-navigation-copy.json`. Five intake rows remain; no deployment or owner aesthetic acceptance is implied.

## Information close to its subject — Tooltip, Hover Card and Chart Tooltip

Tooltip now offers Callout, Shortcut and Annotation; a decorative child owns the real shared contour without weakening the global tooltip exclusion or moving the native trigger. The keyboard hint is functional inside the sample. Hover Card adds Identity, Preview and Media with a bounded organic scrollport; an inline Read preview alternative exposes the complete same content for keyboard/touch use. Personal visual inspection corrected popup padding specificity. Chart Tooltip Compare/Summary/Ranked update stable series rows in place, retain original series colours after ranking and label missing-value subtotals honestly. Large lists use a bounded visible summary and a complete screen-reader alternative rather than an unscrollable pointer-transparent scrollbar; full charts retain their table.

Fresh passes: three native suites (refs, portal, focus/Escape, controlled/disabled/cancel, row identity, missing/zero/negative/empty/large datasets, collision bounds, actual contour motion and reduced motion); actual docs with36 responsive/theme captures; Area Chart consumer regression and seven chart geometry tests; full TypeScript and focused lint; three defaults/12 copied snippets with zero diagnostics;173-item registry build. Delivery also refreshed the Chart package because it owns a second copy of chart.css. Original Tooltip/HoverCard/ChartTooltip/AreaChart installed dependency closures match current source. All nine representative treatments were personally inspected. Evidence: `output/playwright/recovery-20260911-visual-revision/overlay-recovery/` and `overlay-copy.json`. No deployment or owner aesthetic acceptance is implied.

## Identity and small signals — Avatar and Badge

Avatar adds Portrait, Identity and Participants compositions, with locally authored artwork, editable name, image/initials mode and a reversible named roster. The same state survives presentation changes. Shape, accent and three visibly distinct specimen sizes are separate; square/hex/grouped legacy primitives remain demonstrated. AvatarEdit is now a shared44px stationary button, with an adjacent placement for small avatars. Personal inspection corrected portrait scale, edit placement specificity, small-target overlap, control-outline overflow and a low-contrast inherited hex colour in the example.

Badge adds opt-in Stamp, Tag and Counter appearances, preserving all18 original palette/status treatments, native span/ref/children and size contracts. Actions change actual local review state and count. A new painted-path test reproduced corners changing the CSS box but not the visible badge. Explicit morphs can now opt into `data-r="css"`; existing numeric radii and capsule defaults are unchanged. Badge uses that path, including inherited workbench corners and quiet mode. The shared contour,36 light/dark treatment paints, native image success/error fallback, disabled/ref/keyboard/edit target and state continuity all pass.

Fresh checks: identity native suite; actual documentation suite with26 responsive/theme and artwork captures; Activity/Milestone regression; full source TypeScript and focused lint; two defaults/26 copied snippets with zero diagnostics;173-item registry build. Six source-verified payloads delivered; original Avatar/Badge/Activity/Milestone/Button dependency closures match current source. Representative final images were personally reviewed. Evidence: `output/playwright/recovery-20260911-visual-revision/identity-recovery/` and `identity-copy.json`. No deployment or owner aesthetic approval is claimed.

## History and checkpoints — Activity Feed and Milestone Path

Activity Feed receives the requested polish only, not invented variants: caller-owned date groups, connected margin, quieter paper, readable actor/title/time hierarchy and bounded Show more. The actual-docs RED caught a prepended update removing an already-visible entry. Reconciliation now retains visible stable IDs and their edited content before committing, while appended older history still waits for explicit pagination. The native section ref, ordered list, dateTime, empty state, focus-to-first-revealed and announcement contracts pass.

Milestone Path adds Journey, Sequence and Review without replacing its original vertical API. Sequence uses the organic horizontal scrollbar; every layout retains supplied descriptions/status, optional native title actions and caller-owned progress. The local example retains progress and selected detail. Shared current-marker contour motion is verified without moving44px title targets. Personal screenshots caught an inherited grid-column squeezing sequence titles, missing Review bottom spacing and half-painted completed connections caused by normalized dashes plus non-scaling strokes. All were corrected; connections now fade as whole, accurate paths.

Fresh passing gates: both native and actual-docs chronology suites;16 responsive/theme captures; full TypeScript; focused lint; two defaults/five copied snippets with zero diagnostics;173-item registry. Sixteen source-verified affected/dependency payloads delivered, and original closure checks pass13 payloads/94 current source files per component. Final screenshots were personally inspected for feed and all three paths. Evidence: `output/playwright/recovery-20260911-visual-revision/activity-recovery/` and `activity-copy.json`. No deployment or owner aesthetic acceptance is implied.

## Waiting with useful structure — Skeleton and Loading

Skeleton now teaches Article, Profile and Board compositions while keeping all six public placeholder shapes visible. The independent Shimmer, Pulse and Ink effects retain quiet/paused states. The real editable input stays inert and hidden while loading, and its edited value survives loading and template changes. Matching placeholder/content geometry and shared Field/Input/Button parts keep the example useful. Personal visual review corrected a ShapeArtwork sizing override in Board.

Loading adds Bloom, Orbit and Relay mechanisms while keeping the original34px default and Point seed option. The demo is explicitly local, with Pause/Resume and Finish/Restart. Native motion sampling caught initial=false suppressing newly mounted Orbit motion during appearance changes; explicit initial geometry now starts every mechanism. Relay's translation no longer doubles its SVG y-position. Real contours are sampled through their screen transform to verify they stay inside the footprint; quiet, global-off, paused, offscreen and hidden states are still. Native ref/label/children and loading Button activation remain intact.

Fresh checks: native loading suite; actual-docs loading suite with39 captures including ready content/effects; full source TypeScript and focused ESLint; two default/eight copied snippets with zero diagnostics;173-item registry build. Six source-verified payloads delivered, original Skeleton/Spinner/Button dependency closures pass. Representative personal inspection covers every layout/mechanism, content and both themes. Evidence: `output/playwright/recovery-20260911-visual-revision/loading-recovery/` and `loading-copy.json`. These are local checks, not deployment or owner visual approval.

## Messages and measured work — two additional rows

Alert adds Note, Banner and Dispatch while retaining all six semantic statuses as a separate workbench control. One shared morphing stamp replaces competing watermarks. A bounded local Review notes action exposes real sample content; dismissal focuses the surviving restoration action. The previous28px rotating dismiss target is now a stationary44px shared Button, preserving polymorphic native tags, refs, caller role and disabled behavior. Note uses a margin stroke, Banner attaches its action alongside the message at useful widths, and Dispatch leads with a stamp and a small paper fold. Narrow-screen stamp alignment was corrected after personal inspection.

Progress now demonstrates Inline, Report and Milestones around one retained measured value. Organic/Line/Segmented/Orbit, Cream surface and sizes stay independent; unknown progress is explicitly unavailable, not zero. The example removes nested repeated comparison meters and labels stage ranges. Personal screenshots found a real shared paint defect despite correct numeric attributes: synchronous size measurement ran before derived MotionValue subscriptions, freezing the SVG at its100x20 placeholder coordinates. The first size read now runs after subscription setup, with its frame and observer cleaned up. A reproduced1px segmented dash now paints8px at the actual637px viewport. New actual-SVG size/value checks accompany semantics.

Fresh gates pass: Alert/Progress native and actual-docs tests,24 responsive light/dark captures, real contour response for both stamp and measured progress, eight catalogue layout cases and both catalogue behaviors, four progress-geometry tests, full source TypeScript and focused ESLint. Slider motion/range/RTL/vertical/reversal, rubber docs and Questionnaire/AgentChat native contracts were rerun after the shared sizing fix and pass. Copied-source coverage: three defaults/24 snippets including Slider, zero diagnostics. Registry build173, seven source-verified affected/dependency payloads delivered; original closure checks pass Alert/Progress/Slider/Questionnaire and served Alert/Progress/foundation payloads match. Evidence sits under `output/playwright/recovery-20260911-visual-revision/`. Every approach was personally inspected representatively;16 component rows and shared-shell work remain open. These are local implementation checks, not owner visual approval or deployment.

## Collection and reading continuity — two additional rows

Carousel now presents Shelf, Story and Index: peeking authored cards, a full illustrated reading spread, and a visible thumbnail chooser. Four distinct field-note compositions share local save state and selected position across layouts. Existing native Content/Item Slot and Deck APIs remain. An optional owned horizontal ScrollArea keeps the native viewport/ref and uses the shared organic thumb. Reproduced defects included re-enabled boundary actions, undersized changing dot targets, inherited RTL direction and decorative overflow for a fitting single card. Fresh tests cover owned/legacy LTR/RTL, inherited RTL, Home/End, cancelled events, nested input arrows,44px stationary dots, empty/single, quiet settings and rapid smooth-scroll reversal.

Reading Trail now has Spine, Bookmark and Overview. The same article and scroll position survive composition changes. Bookmark adds current chapter, bounded previous/next and reversible contents using the shared disclosure. Native fragments/modifier clicks, destination focus/restoration, nav ref, deduplicated IDs, missing/empty sections and bounded local progress remain intact. The original TOC keeps its omission default; a reproduced3px native-link hover translation is removed globally. Personal screenshots also caught plain ScrollArea styling overriding the intended reading paper: a scoped surface now separates article text from the preview pattern. Its quiet marker and themed chapter emphasis remain legible.

Passing evidence: both native browser suites; both actual-docs suites with24 responsive light/dark captures; four Reading Trail/Living Link unit/SSR cases; full source TypeScript and focused ESLint; two default/eight copied snippets with zero diagnostics. Full registry build173 items, nine source-verified affected/dependency payloads delivered. Original closure checks pass seven payloads/82 current source files for each component; served Carousel/Reading Trail payloads exactly equal delivered originals. Evidence and the combined copy receipt are in `output/playwright/recovery-20260911-visual-revision/`. The primary agent inspected every approach representatively, corrected material findings, and recaptured; owner aesthetic acceptance is still separate. Eighteen component rows and shared-shell/global-scrollbar work remain open. No deployment or fresh full production-build claim.

## Adjustment and guided progression — five additional rows

NumberInput now has Stepper, Quantity and Scrub arrangements around the same editable native input. The scrub grip uses bounded steps with click-to-edit and keyboard alternatives; min/max/step, locale, precision, controlled refusal, refs, accepted/cancelled form reset and44px stable targets were checked. A cancelled-reset defect and a6px decorative overflow were reproduced and corrected. OptionWheel adds Reel and Compact to Arc, while keeping side placement separate. Actual wheel/trackpad input is bounded, ignores pinch/horizontal gestures and releases page scrolling at either end. Explicit buttons/keys retain their wrap contract. The live test measures changing contour paint and a stationary held row. The24 final captures use actual data-mode light/dark, not an ineffective class toggle.

Questionnaire now teaches Stacked, Journey and Worksheet using two real questions, retained answers, progress, disabled choices, local save/reset and deliberate next-heading focus. Six shapes, selected marks and sizes remain independently usable. Native label press no longer shrinks the target. Accepted and cancelled form resets retain agreement between checked inputs and their painted state; controlled values remain authoritative. AgentChat's legacy questionnaire caller passes its regression check. Visual inspection caught two defects missed by the first behavioral pass: both question bands inherited pink, and wrapped titles fell beneath the numbered tabs. Scoped paint variables now follow pink/blue selection while retaining legacy fallbacks, and a separate title column keeps every heading aligned. Strict rendered-colour and wrapped-heading tests pass;12 fresh captures were inspected representatively in both themes.

Breadcrumb has Trail, Pocket and Directory compositions, genuine ancestor links, a keyboard/Escape ancestor menu and a working parent action. The pocket path deliberately separates parent context from the current page; separators in a full trail stay with their destination when wrapping. Back has a44px stable target. Stepper has Rail, Ledger and Compact around one preserved draft: validation gates progression, earlier stages remain reachable, focus follows deliberate stage changes, and save is explicitly local. Caller disabled=false cannot re-enable first/last boundaries; cancelled events, non-finite values, controlled refusal and refs were checked. The selected marker uses the shared changing contour without moving its button. The compact progress marks and checkpoint labels were corrected during personal visual review. Native breadcrumb Slot/ref remains one anchor; optional separator Slot supports valid grouped wrapping.

Fresh passing gates: adjustment docs/native plus24 focused numeric/appearance tests; questionnaire docs/native; progression docs/native; full source TypeScript and focused ESLint. The shared Choice docs matrix was freshly rerun after questionnaire paint changes and still preserves all six glyphs and marks. The latest combined copied-source gate covers Questionnaire/Breadcrumb/Stepper: three defaults,21 snippets, zero diagnostics. The earlier adjustment gate covers two defaults/eight snippets. Registry rebuilt173 items; eleven source-verified affected/dependency payloads were delivered for the latest group (the earlier adjustment closure had eight). Original-checkout closure checks pass all five new rows plus AgentChat, Checkbox and RadioGroup. Served Questionnaire/Breadcrumb/Stepper/foundation payloads equal the delivered originals. No deployment or fresh production-build claim.

Evidence: `output/playwright/recovery-20260911-visual-revision/` holds24 adjustment,12 questionnaire and24 progression captures and their copied-source receipts. Representative personal visual review covers every approach; capture count is not owner aesthetic approval. Thirty-five rows now have local evidence; twenty component rows and shared-shell/global-scrollbar work remain open. Earlier counts below are historical checkpoints.

## Latest visual revision and selection checkpoint

The owner found Accordion insufficiently artistic. Primary inspection confirmed that FAQ and Chapters explicitly disabled shared contour motion, and their layout read as ordinary divider rows. Removing that opt-out alone did not restore motion because the default card-motion category is disabled. The documented approaches now deliberately own shared paint-only response, while reduced motion and Motion Off remain quiet. FAQ uses soft answer slips and a true plus/minus indicator; Chapters uses numbered folded tabs and a stitched continuation line; Editorial uses a coloured spread with a shared morphing silhouette and readable detail. The legacy omitted-appearance contract remains. Pointer response changes the outline, never the native hit rectangle. No extra idle animation loop was added.

Fresh `disclosure-recovery.docs.browser.mjs` passes the actual three approaches, retained open state, native keys, real interruptible height, changing contour path, held-press geometry, reduced motion and12 theme/responsive captures. Primary visual review covered FAQ light, Chapters dark and Editorial light/mobile-dark. `disclosure-slots.test.tsx` still passes both Slot/default-indicator contracts. The craft detector's radius/type-size advisories were considered, not reported as a clean automated aesthetic approval. Existing legacy geometry was preserved.

MultiSelect now exposes Tokens, Summary and searchable Checklist without hiding optional item art or selected values. Select supplies Field, Action and Rich choices with descriptions excluded from selected-value text. NativeSelect supplies Field, Toolbar and Listbox while retaining the native platform popup. Real native tests pass controlled refusal, disabled/unknown values, refs, forms and both accepted/cancelled reset. Select's opaque trigger was covering its shared painted contour; the host is now transparent only when that contour exists, and the live test checks it. All three real-docs tests pass state preservation, configured copy, independent radius and responsive themes.

The fresh runtime is an exact-lockfile source snapshot at `/private/tmp/cojeev-recovery-m9KWHR`, served on4321. The previous temporary snapshot was gone; original dependency reads stalled, so dependencies were restored from the local cache only in this disposable runtime. Original dependency files were not changed. Full source TypeScript and focused Accordion lint pass; the latest focused copied-source gate covers four defaults and16 snippets with zero diagnostics. The previous broader198-snippet gate is historical evidence, not a newly rerun whole-library gate. The registry rebuilt173 items;43 affected payloads were source-verified before delivery, and the original-checkout closure checker freshly passes Accordion/MultiSelect/Select/NativeSelect. No production build, deployment or stranger-install claim.

Fresh Choice, Card/Collapsible and Forms docs checks also pass. Their representative visuals were personally rechecked, but this does not close the owner's broader quality concern. Current captures are preserved in `output/playwright/recovery-20260911-visual-revision/`, separately from earlier images so before/after evidence is not erased. Twenty-five intake rows plus shared-shell work remain open.

The primary assistant performed implementation, browser checks and representative visual inspection directly. No sub-agents, commits, pushes, deployment, dependency changes or blanket rollback were used in this recovery. Existing unrelated changes remain intact.

## What changed

| Component | Delivered correction | Try it |
| --- | --- | --- |
| Checkbox | Restored all six existing glyph shapes and Auto/Dot/Check/Diamond/Flower/None mark controls. Row/Card/Chip remain separate approaches. Selected values survive presentation changes. Removed the competing card illustration. Row no longer offers meaningless Corners; Card/Chip expose Container corners. | Choose Card, change Glyph shape, then Selected mark. Toggle Working notes; changing appearance must not reset the chosen tools. |
| Radio group | Same six silhouettes and mark controls restored independently of the group approach. One-of-many state, native keys and selected-value summary retained. | Choose a different starting point, then change shape or approach. The selected value stays intact. |
| Accordion | FAQ, Chapters and Editorial are three directly visible compositions. Added a shared interruptible height owner, directional indicators and closed-content focus isolation. Legacy appearance remains when the new prop is omitted. | Open the second section, switch approach, use ArrowDown/Space, or reverse an opening/closing transition. |
| Card | Editorial reading card, actionable Project summary and browsable Collection. Original ten surface treatments and sizes are independent controls. Useful local actions, scoped corners and touch-sized CardAction. | Save the article, complete a project step, or browse the collection. Change Surface without losing that state. |
| Collapsible | Inline detail, Checklist and Inspector. Visible indicator, touch-sized trigger and the same reversible height owner as Accordion. Native/custom `asChild` trigger contracts retained. | Open using Space, follow the detail link, or quickly reverse a transition. Closed descendants are inert. |
| Toast | Compact, Actionable and Receipt layouts. Intrinsic-width Save trigger, readable title/body/action hierarchy, local Undo and touch-sized dismissal. Example viewport is embedded; exported viewport still defaults to fixed placement. | Save, Undo, save again, dismiss, then reopen. Focus stays at the initiating button. |
| Hero Button | Twelve concepts: Liquid, Split Arrow, Paper Fold, Pressed Key, Ribbon, Ticket, Seal, Ink Sweep, Orbit, Sliding Window, Stacked Paper and Spotlight. Organic/Capsule entry points retained, so the gallery has thirteen entries. | Hover or keyboard-focus each concept and activate its local action. Motion affects paint/arrow/label, not the native hit rectangle. |

Local verified routes use `http://127.0.0.1:4321/cojeev-ui/docs/` with the IDs `checkbox`, `radio-group`, `accordion`, `card`, `collapsible`, `toast`, and `hero-button`. The repeatable runtime is the exact-lockfile source snapshot at `/tmp/cojeev-bento-dependencies-nVPN37`. This checkpoint does not claim port4320 was freshly verified.

## Failures caught during this implementation

- Initial real-docs tests failed on missing shape controls, missing Accordion/Card/Collapsible approaches, the stretched Toast trigger and the two-entry Hero gallery.
- Adding a default disclosure indicator initially violated Radix's one-child `asChild` contract. Fixed both disclosure triggers; dedicated SSR checks now cover the native caller-owned element.
- The first Hero treatment CSS lost to older size rules: Seal became a pill and folded-paper corners disappeared. Source-specific selectors were corrected, with a real 1:1 Seal geometry assertion added.
- The ordinary Button default is40px, not44px. CardAction now explicitly supplies a44px minimum; the new live test caught the incorrect assumption before handoff.
- Receipt metadata originally used page ink on alternate Toast surfaces. Explicit semantic primary/secondary ink now follows default/cream/danger palettes; the regression test reproduces and checks that case.
- Copied/downloaded source is a separate gate. Only the verified thirteen-payload dependency closure was delivered back from the build snapshot, preserving unrelated registry entries.

## Fresh passing checks

Run with Node22.22.0 from the snapshot for browser/dependency stability. The download-freshness check also passed from the original checkout after delivery.

| Gate | Evidence |
| --- | --- |
| `tests/choice-recovery.docs.browser.mjs` | Real registered docs: six rendered silhouettes, selected-mark controls, value preservation, precise corner scope, configured Copy, native interaction and24 responsive/theme captures. |
| `tests/choice-native.browser.mjs` | Native form values, mixed state, disabled-item keyboard skip, controlled refusal/ref behavior and stationary switch target. |
| `tests/disclosure-recovery.docs.browser.mjs` | Three Accordion approaches, retained open state, keyboard, repeated reversal, sampled real height motion and12 captures. |
| `tests/card-collapsible-recovery.docs.browser.mjs` | Six structural approaches, useful actions, retained state,44px Card actions, hidden-content isolation, actual height reversal and24 captures. |
| `tests/disclosure-slots.test.tsx` | Two SSR tests: one caller-owned native trigger through `asChild`; one indicator on a standard trigger. |
| `tests/toast-recovery.docs.browser.mjs` | Three layouts, bounded trigger, local save/Undo, focus retention,44px dismissal, keyboard/reopen, semantic receipt ink and12 captures. |
| `tests/hero-button-recovery.docs.browser.mjs` | Thirteen live gallery entries, all twelve concepts, retained click/Space counts, stationary hover rectangles, Seal geometry, reduced motion,52 responsive/theme captures plus a keyboard-focus capture. |
| `tests/hero-button-native.browser.mjs` | All twelve public appearances: button/link semantics, forwarded ref, exact activation count, disabled/loading blocking, disabled-paint precedence, Motion Off and Flow Off. |
| Copied examples | Seven default examples and90 variant/size/configuration snippets: zero extraction or TypeScript diagnostics. |
| Source gates | Full TypeScript check passed. Focused ESLint passed for changed component/example/motion sources and the disclosure slot tests. No full-repository lint claim. |
| Registry |173 items built successfully. `scripts/check-recovery-downloads.mjs --components=checkbox,radio-group,accordion,card,collapsible,toast,hero-button` verifies current source content, install targets and the complete local-import dependency closure for each. No production-site build, stranger install or publication claim. |

Captured evidence is preserved in `output/playwright/recovery-20260911-LsqHR1/`: choice-recovery, disclosure-recovery, card-collapsible-recovery, toast-recovery, hero-recovery and copied-source.json. There are125 image captures. The primary assistant inspected every Hero concept and representative choice/disclosure/card/toast captures, including desktop/light and mobile/dark. Capturing every theme/width does **not** mean every image received an individual aesthetic certification. Owner visual acceptance remains separate.

## What remains open

The seven-family results above are the first checkpoint. The next seven rows have now been completed as recorded below; the remaining families remain open.

## Chart checkpoint — seven additional rows

Charts introduction, Area, Bar, Line, Pie, Radar and Radial now have component-specific verification. Six chart galleries expose Analysis, Brief and Ledger as genuine compositions, while every existing chart geometry remains independently selectable through Chart mode. Dataset and hidden-series state survive presentation changes. ChartDatasetSelect uses the actual Cojeev Select and ships its dependencies. The Charts introduction now teaches one complete, inspectable frame rather than disconnected marks.

Visible values use a bounded custom-scroll TableContainer. Source summaries explicitly describe their measure and observed-value denominator; missing values are not zero. Radar now breaks paths at missing observations instead of inventing center points or a closed filled polygon. Its generated Next week sample is bounded by the stated100-point scale.

Fresh checks: `tests/chart-recovery.docs.browser.mjs` passed all six routes and the introduction; the stricter Radar scale case then failed before correction and passed on a focused rerun. Coverage includes all geometry controls, three compositions, real dataset menu, state preservation, keyboard inspection, five datasets, finite geometry, repeated ordinary-motion updates without plot remount, custom table scrolling and73 captures. `tests/chart-suite.test.ts` passed seven geometry tests including missing Radar gaps. Full TypeScript and focused chart-source ESLint passed. Combined copied-source gate:14 defaults,115 snippets, zero diagnostics. Registry rebuilt173 items; all fourteen component payload closures match current source, and the served4321 payloads match the delivered files.

The verified28-payload dependency closure was delivered without replacing unrelated registry entries. Chart captures and `copied-source-fourteen.json` are preserved alongside the first checkpoint in `output/playwright/recovery-20260911-LsqHR1/`. Primary visual inspection covered Area Brief and bounded mobile Ledger, Bar Brief, Line Ledger, Pie Brief, Radar Brief, Radial mobile Ledger and the introduction. Screenshots alone are not owner aesthetic approval.

## Navigation checkpoint — three additional rows

Pagination now has Pages, Chapter and Jump compositions. Numbered targets use a fixed window; current ink updates locally, without a travelling fill. The native button keeps its rectangle even during a held press: the shared opt-in `data-stable-hit` adapter scales only its decorative SVG. Jump validates whole pages and reports invalid input. Tabs retain Pills/Underline/Lenses and add Notebook and Rail. Managed outgoing/incoming panels occupy one grid position; exited content stays outside focus/accessibility. Navigation Menu now has grouped Directory, real Shelf and Compact menus, native destinations and visible local results. Pending hover cannot reopen a dismissed menu after Escape; a fresh entry can open it again.

The stricter visual pass found React className replacement erasing the shared Flow owner's positioning marker during appearance changes. This made decorative highlights become real flex items and push tab labels out of view. The motion owner now restores only its own marker, with a same-kind appearance-change regression check. Theme ink on tabs is immediate rather than interpolating from the previous theme. Chart metadata, search breadcrumbs and navigation order now consistently use the dedicated Charts category; eight chart entries including the inspector are tested.

Fresh checks: `tests/navigation-recovery.docs.browser.mjs` (three Pagination and five Tabs approaches;32 captures), `tests/navigation-menu-recovery.docs.browser.mjs` (three menus;12 captures), `tests/navigation-native.browser.mjs` (controlled refusal, numeric bounds, Slot/ref/native links, stable held-press bounds, disabled tab skip, Escape/focus and quiet modes). Hero and choice native tests passed after the shared press adapter; full TypeScript, focused lint and category/discovery checks passed. Combined seventeen-row copy gate:129 snippets, zero diagnostics. Primary visual review included each navigation approach, both new tab layouts and the corrected small-screen retained styles. These checks do not certify all application cursor interactions.

## Tables checkpoint — one paired component row

Table and DataTable now have one discoverable Tables guide. The old `/docs/data-table/` URL renders the combined guide with the canonical Tables link; both public modules and all exports remain installable. Sidebar and Fumadocs search expose one entry, including searches for DataTable. The guide explains and documents both APIs. Ledger is a compact sortable/filterable register; Rich rows supply context and explicit Open/Pin actions; Comparison pivots records into columns with proper row headers and group navigation. Zero remains visible. Selection, pinned records and the composed view's state survive presentation changes. Wide tables use the actual custom horizontal scrollbar; keyboard scrolling is checked at390px.

The reported flicker was reproduced frame by frame: switching pages retained three outgoing rows alongside two incoming rows. Those exit wrappers were removed from table flow. Records now replace immediately, while shared controls retain paint feedback. Nested controls do not also activate their row. Focused rows have an explicit outline without relying on a decorative icon. Empty feedback lives inside the table, and one-page results do not show redundant pagination.

Fresh checks: `tests/table-recovery.docs.browser.mjs` (legacy/canonical guide, three approaches, repeated page state, sort/filter, Open/Pin, zero values and12 captures), `tests/table-native.browser.mjs` (row IDs, native keyboard/focus, nested actions, controlled page/filter refusal/ref, empty recovery, sort direction, Motion Off and Flow Off), `tests/table-discovery.test.ts`, and `tests/table-scroll.test.ts`. Area and the Charts introduction were rechecked after the shared TableContainer change; the seven chart geometry tests also passed. Primary visual review covered all three approaches and mobile scrolling.

The combined checkpoint covers19 installable example IDs because Table and DataTable share one intake row:19 defaults,137 copied snippets, zero TypeScript diagnostics. Full source TypeScript and focused lint passed;173 registry items rebuilt. All19 component payload dependency closures match active source, and their served4321 payloads equal the delivered files. The33-payload affected closure was refreshed, preserving unrelated entries. Added evidence:44 navigation captures,12 Table captures and `copied-source-eighteen.json` under `output/playwright/recovery-20260911-LsqHR1/`. There are254 captures across the checkpoints, not254 individual aesthetic approvals.

## Selection-control checkpoint — four additional rows

Fresh real-docs testing found Calendar and Date Picker missing their mode selectors: their variants were not in the permanent catalogue additions, so a rebuild erased discoverability. Both now expose Single/Range/Multiple. Calendar's previous highlight moved but retained a fixed outline; the stricter contour assertion failed before correction. Its finite selected paint now interpolates related organic contours, without moving the day button. Quiet mode retains the date-dependent shape immediately. Existing required/min/max/disabled, caller-controlled values, month navigation and Date/DateRange/Date[] contracts remain intact. Date Picker's shared field corners were live-tested and already worked, so they were preserved rather than replaced.

Rubber already existed, but its actual waist is now measured: approximately15px at12% extent versus1.875px at100%, even with reduced motion. The initial probe sampled an intermediate ResizeObserver path; measuring after two paint frames distinguishes that from a persistent geometry failure. Drag reversal, range/RTL/vertical, native ref/form/commit and quiet settled-shape equivalence passed. Switch's Capsule/Rocker/Latch mechanisms were personally inspected; actual docs and native tests cover click/Space, controlled refusal, submitted values, disabled state,44px hit targets, stable held presses and quiet behavior. No redundant Switch redesign was added.

Fresh checks: `tests/calendar-modes.browser.mjs` (all modes, cross-month range, disabled/keyboard/Clear/Done/Escape/focus, selected contour and24 captures), `tests/calendar-native.browser.mjs`, `tests/slider-rubber.browser.mjs` (four captures), `tests/slider-motion.browser.mjs`, six slider geometry tests, switch-only `tests/choice-docs.browser.mjs` (twelve captures), and `tests/choice-native.browser.mjs`. Full TypeScript, focused lint and23 defaults/156 copied snippets passed. Registry rebuilt173 items; the37-payload affected closure matches active source. All23 recovered example IDs pass original-checkout source/import-closure checks; the four new served4321 files equal their delivered originals. Forty additional images and `copied-source-twenty-two.json` are preserved in the recovery evidence folder. Primary visual inspection covered all Calendar/Date Picker modes, all Switch mechanisms and short/long Rubber; owner acceptance is separate.

## Form checkpoint — five additional rows

Input retains Contour/Editorial/Inset and its native editing/clear contracts. Field now explains its distinct role through Stacked, Inline and Integrated layouts, preserving both help and validation associations. Input Group demonstrates Address, Quantity/unit and Composer with real local actions. Textarea has Writing, Composer and Limited response arrangements; all preserve the draft and its240-character allowance. Label teaches Required/Optional/Help with visible labels targeting the actual native input. Surface and corners are independent where meaningful; Editorial does not offer a no-op Corners control. Presentation changes keep entered values and errors.

The visual review caught redundant inner composer borders; one surface now owns the frame. A held-press native test caught Clear moving down1px despite its stationary-hit setting; the shared Button opt-in now suppresses that leftover active transform. InputWrapper and Input Group also keep their editing rectangles stationary while decoration responds. Token/glyph corners are not confused with field geometry.

New ElementScrollBar reuses the existing organic thumb against an explicitly supplied native scroll element. TextareaScrollArea attaches it without replacing the textarea, preserving refs, form submission, caret, selection, input events and native resize. Its marker is reference-counted and removed on teardown; null never targets the document. Native bars remain before mount, in forced colours and when unwrapped content requires horizontal scrolling. This is scoped textarea coverage, not a completed global migration.

Fresh passing checks: `tests/form-recovery.docs.browser.mjs` covers all five actual pages, fifteen approaches, labels, state/error continuity, real textarea keyboard scrolling, radius scope, configured copy and60 responsive/theme captures. `tests/form-native.browser.mjs` checks native refs/form/selection, clear prevention and controlled refusal, help/error associations,44px Clear and held-press bounds. `tests/textarea-scroll.browser.mjs` checks native editing/resize, organic Home/End/drag, input/controlled growth, forced-colour and horizontal fallback, and StrictMode cleanup. `tests/form-foundations.browser.mjs` passes the previous foundation/native editing matrix and actual Input pages. Shared global-scrollbar and scroll-appearance checks, navigation-native and calendar-native checks passed; seven focused field/scroll unit tests passed. Full source TypeScript, focused ESLint and28 default examples/186 copied snippets passed with zero diagnostics.

Registry rebuilt173 items; the40-payload affected closure was source-verified and delivered while preserving unrelated entries. All28 recovered example IDs pass original-checkout source/import checks. The seven newly changed served4321 payloads match their delivered originals. Sixty form-page and24 foundation captures plus `copied-source-twenty-seven.json` are preserved in the recovery evidence folder (an earlier debug capture is explicitly not acceptance evidence). Primary visual inspection covered each form approach, including revised composer frames and label semantics. Owner aesthetic acceptance remains separate.

## Still open after twenty-seven rows

The [original intake](2026-09-10-library-overhaul-intake.md) retains the remaining28 component rows and shared requests. In particular: Breadcrumb, Carousel, Reading Trail, standalone Sidebar and Stepper; MultiSelect/Select/NativeSelect/NumberInput/Questionnaire/OptionWheel; Alert, Progress, Loading and Skeleton; identity, tooltip/hover-card/item treatments; unified Shape/Icon studios; Dialog and reference-first Linear Modal; and shared-shell/global-scrollbar acceptance. Previously implemented compact navigation and Bento work requires its own current acceptance rather than automatic reimplementation or closure.

Do not mark these rows done using the twenty-seven-row results above. Each subsequent batch starts with a reproduced named behavior failure, preserves restored capabilities, and carries its own real-docs, native-interaction, visual and install evidence.
