# Reporting stack correction — 2026-09-10

Supersedes Round 9's visual composition, not its verified draft/persistence behavior. The user rejected three separated boxes. The sidebar redesign is paused while this correction is implemented; its RED test is not a shipped regression.

## Direction

- THESIS: two paper files, one working surface. The alternate task is physically within reach.
- OWN-WORLD: the inherited Intelly/Cojeev rule of connected geometry; warm surfaces, ink type, pink and blue file edges. Current runtime tokens override older handoff prohibitions on all shadows/gradients, which later user requests superseded.
- STORY: open → cards arrive separately → read the heading on the front card → write → select the other clearly labelled file edge → return with the draft intact.
- FIRST VIEWPORT: integrated file handles, one card heading and close control, concise truthful service state, the first meaningful fields. No separate title plaque or detached segmented toolbar.
- FORM: local redesign of the established component, preserving its source-based drawer mechanics. No new brand, generated artwork, or invented approved composition. Hover can extend a peek; it cannot be required to discover or switch cards.

## Work and evidence

16:55 user continuation: attached handles still have square ends meeting rounded card corners, creating disconnected notches in both selected states; the inactive label is partly covered. This is part of the same composition correction, not a separate approved design. The handle and its sheet must share a continuous painted silhouette. Verify seams and actual pointer hit targets in both directions; counts and tab semantics alone do not verify this geometry.

- [x] Shared MotionDrawer fixes the composition for panel-stack consumers; focused keyboard/RTL/quiet-motion, controlled state, legacy-sheet and stagger checks pass.
- [x] Global scrollbar defaults flow through shared scroll components; unmanaged/native regions receive a documented fallback without DOM reparenting.
- [x] Report draft saving, independent fields/files, review, pin/capture consent and disconnected-send restrictions still pass.
- [x] Capture and inspect desktop/mobile light/dark; review appearance separately from behavioral tests.
- [x] Whole-library inventory and source audit distinguish scan coverage from actual review and browser evidence.
- [x] Reusable component-craft skill, creative repertoire and design decisions record the repeated feedback.

Verification: `reporting-continuity.browser.mjs` checks twelve selected/return states at 1440px and 390px, light/dark: joined corner pixels, aligned outer edges, unobscured handle centers and label lines, and both handle contrast ratios of at least 4.5:1. Independent visual review found the neutral dark handle's fixed dark ink; card-owned surface/ink pairing fixes that defect. `reporting-stack.browser.mjs`, draft migration/persistence, global scrollbar checks, 231 unit tests and typecheck pass. The copied-source check reports six snippets with zero TypeScript diagnostics, but its overall gate still fails on the existing unregistered `semantic-bloom 2` artifact.

Scope remaining: the audit inventories all 170 components but directly reviews 29; it is not a full visual/performance certification. Deeper audit findings and the sidebar redesign remain pending. No production publication or user visual approval is claimed.

Final finish receipt: the independent reviewer rechecked the corrected desktop/mobile dark captures and reported no remaining material fixes in this review. The legacy drawer browser suite also passes for all four variants at desktop/mobile light/dark, including drag, focus, reduced motion, actions and bounds. Final lint and diff checks pass.

Ponytail is installed as skills only. Its audit is advisory and read-only; performance/correctness are a separate review. No lifecycle hooks, MCP service, new app dependency, publication, or deletion of unknown duplicate artifacts is authorized by this implementation.
