# Cojeev UI rapid fixes

Started 2026-09-10. Local review: http://127.0.0.1:4320/cojeev-ui/

## Final recovery checkpoint — 2026-09-11

Primary-agent implementation and local verification now cover all 55 overhaul intake rows, the separate twelve-concept Hero Button collection and scoped shared-shell requests. Current verified runtime is port 4321. See [recovery delivery](2026-09-11-recovery-delivery.md) for the complete family record, retained original controls and precise limits. Historical checkpoints below are not current completion counts. No sub-agents, commits, publication or project dependency changes were used in this recovery; owner aesthetic acceptance is separate.

### RF-U-041 — Bento resize oscillates while the pointer is nearly still

- Source: owner's `Screen Recording 2026-09-11 at 11.57.22 AM.mov`; `/docs/bento-grid/`, resizing a shared seam. Status: reproduced, repaired and verified locally. Owner: primary assistant.
- Symptom: the selected tile alternates between two and three rows; neighbouring tiles and the document jump; the visible grip lags behind the live edge.
- Root cause: editor tracks used content-sized fractional rows. A neighbouring one-row tile's intrinsic text height changed the complete grid from roughly 410px to 654px. Pointer normalization against that changing canvas fed a different row back into the next preview. Grip geometry still referenced the committed tile.
- Fix: `registry/cojeev/styles/bento-builder.css` gives the editor stable 144px row tracks and size-contained, height-aware label typography. `registry/cojeev/ui/bento-builder.tsx` positions grips from the live preview while preserving their captured native elements. Consumer BentoGrid stays fluid; exported layout data is unchanged.
- Verification: new `tests/bento-resize-stability.docs.browser.mjs` failed on measured alternating heights, then passed fourteen near-stationary samples in both Classic and Interlock, stable canvas/row count, grip-edge agreement, commit and Undo. Full native builder and actual-docs regressions pass, including invalid-movement feedback, cancellation, keyboard/copy and responsive themes. Final held-drag/mobile captures were personally inspected.
- Evidence: `output/playwright/recovery-20260911-visual-revision/final-integration/bento-resize-stability/frames.json`, held-state images and the Bento native/docs capture folders.
- Delivery: full registry build 173 items; 79 affected/dependency payloads source-verified into the original checkout. All 65 selected-root import/dependency closures and 374 copied snippets pass. No production-build or deployment claim.

## Working agreement

User reports take priority. Reproduce the symptom, identify its cause, compare related components, apply the scoped fix, and verify the original interaction. Preserve new incoming reports while work is running. Compare repeated reports by component, interaction, symptom and cause; link duplicates to the original issue and reopen regressions.

Status: reported -> reproducing -> fixing -> needs verification -> verified. A code edit is not a verified fix. Use blocked only with a concrete blocker. Duplicate issues retain their original report and link to their canonical issue.

Each issue records: stable ID, source (user/proactive), component(s), exact route and reproduction, symptom, expected behavior, root cause, affected/shared paths, owner, fix, verification evidence, related/duplicate IDs, remaining limits.

## Audit coverage

2026-09-10 later census: **170 registered UI components**. The original 160-component family assignment below is historical; the refreshed per-component coverage is in [whole-library-audit.md](whole-library-audit.md). Inventory, static review, browser checks and visual approval remain separate.

All 160 registry UI components are assigned exactly once. Per-component checks and findings live in the group records below. Queued does not mean checked. Source review, automated tests, browser interaction, responsive visual checks and variant coverage are recorded separately.

- Controls: 61 components; [coverage and findings](controls.json)
- Motion and visuals: 56 components; [coverage and findings](motion.json)
- Data, conversation, compositions and tools: 43 components; [coverage and findings](data.json)
- Coordinator: user intake, shared documentation shell, cross-family causes, review and integration.

Agents own leaf files for their assigned components. Before touching shared helpers, styles, examples or tests used by another group, send an exact path claim to the coordinator and wait for ownership assignment. The coordinator owns this ledger, app/docs, components/component-preview.tsx, shared example manifests, registry.json, public/r, data/component-guides.json and data/component-additions.json. Generated registry refreshes run once under the coordinator after integration.

Use independent hidden browser tabs and do not navigate the user's review tab. Preserve existing untracked AGENTS.md, CLAUDE.md and Semantic Bloom files ending in ' 2'. No commit, push or publication has been requested for this round.

## User reports

### RF-U-BENTO — Useful Bento builder replaces Aspect Ratio showcase emphasis

- Source: user request and approval of recommended neighbor-resize design. Status: reopened2026-09-11 after live drag review; earlier bounded verification retained below, not current resize acceptance.
- Added four templates, seeded randomization, Classic/Interlock treatments, complementary wavy seams, seed-only animated Reshape edges, rows/columns, drag swaps, seam resizing, keyboard/button alternatives, editable labels, Undo/Redo and exact layout copy. Tile count stays fixed; dimensions that would lose content are rejected. AspectRatio utility and route retained for compatibility.
- Independent review approved after fixing external-focus Escape, invalid renderer native props/ref, mounted outer-treatment synchronization and narrow-tile typography.
- Verification2026-09-11:286unit tests pass; TypeScript passes;7copied snippets pass;173-item registry build passes. Both download dependency graphs verified against original source after delivery. Real docs at1440/390px in both themes pass with one editor, working treatment/reshaping controls, unclipped canvas and no page overflow/runtime errors. Focused browser suite covers pointer, keyboard, resize, history, copy and quiet/normal motion.
- Preview: http://127.0.0.1:4321/cojeev-ui/docs/bento-grid/ uses exact-lockfile/source snapshot under /tmp/cojeev-bento-dependencies-nVPN37. Original4320 file-read stall was bypassed, not fixed. Mac was locked during foreground access. No commit/push/publication; this does not close the older whole-library overhaul.
- Fresh primary-agent review2026-09-11 on that exact4321 route: Editorial, first tile, bottom handle, pointer held100px below starting boundary. Repeated moves at the identical pointer coordinate alternate tile row-span2/3 in both Classic and Interlock. Classic board height alternates410/510.3125px; Interlock380/512.3125px. `move()` divides pointer position by the newly measured board height, while intrinsic content changes that height as the preview resizes. This feedback loop reproduces flicker and apparent missed resizing even under reduced motion.
- Additional confirmed UX defects: handles are positioned from committed `tile`, not preview tile, so width drag handle stays at its old boundary until release; no feedback before crossing half a grid cell (about65px horizontally in the1440px docs test); first tile cannot expand right because adjacent tile has width1, but rejection only appears in footer. Pointer release can replace rejection with misleading “Shared seam moved” even if unchanged. Browser move/read rounds were roughly27–47ms; this does not establish a general performance profile, but the reproduced failure does not require an intentional timer or debounce.
- Scope of the drag review: review/check only; no component code changed during that review. Corrective direction: use gesture-start coordinates/scale, render handles from preview geometry, communicate snapping/blocked direction near the handle, and preserve accurate no-op/rejection feedback. Add a stationary-coordinate regression in the real multi-tile docs layout; the existing two-tile large-drag fixture does not catch this content-dependent loop.
- Follow-up requested and implemented2026-09-11: blocked movement now shows a pointer-transparent, viewport-bounded explanation beside the pointer or keyboard/button control, with the existing polite status announcing the reason. Rejected pointer release retains the reason rather than announcing a false move; overshooting commits only the last valid size and explicitly says so. Valid movement, a new selection, Escape, history changes and scrolling dismiss stale visual feedback. Non-tile drops explain where to drop without treating ordinary clicks as errors. No-op release explains cell snapping.
- Feedback verification: initial nearby-feedback regression failed (0 hints instead of1), then passed after implementation. Expanded browser suite passes Classic/Interlock rejection, no extra history, last-valid preview, recovery, Escape, invalid drop, keyboard resizing, desktop/mobile light/dark bounds and pointer transparency. The actual4321 docs route passes both treatments, release preservation and no page errors.286unit tests, TypeScript and7copied snippets pass;173-item registry rebuilt, builder payload and its react-dom catalogue declaration refreshed, both download graphs retain source parity. Screenshots: output/playwright/bento/blocked-*.png and docs-blocked-*.png in the4321 snapshot.
- **Still open:** the content-dependent height/coordinate feedback loop, committed-versus-preview handle placement and half-cell snap threshold. This feedback-only change does not repair or close those geometry issues, nor the whole-library overhaul. No commit or publication.

### RF-U-001 — Documentation visual character

- Source: user screenshot and review of /docs/.
- Status: verified locally; visual approval remains with user. Owner: coordinator.
- Symptom: plain documentation does not express the visual quality of the component library.
- Expected: use existing shaders, organic shapes and animation across the docs while preserving reading clarity and responsive controls.
- Cause: docs shell uses only flat neutral surfaces and imports none of the existing background/artwork components.
- Fix: shared PigmentField backdrop, composed ShapeArtwork/FloatLayer headers, stronger page hierarchy and surface spacing. Applied to getting started, every component detail page and the reference guide. Reused the library's visual language instead of creating an unrelated theme.
- Paths: components/docs-atmosphere.tsx, components/docs-shell.tsx, app/docs/docs.css, app/docs/page.tsx, app/docs/[component]/page.tsx, app/docs/reference-guide/page.tsx.
- Verification: desktop 1280x720 light/dark visual checks; no horizontal overflow at 1280px or 390px. At 390x844, Browse focuses search, Compact dashboard search returns its Composition link, following it closes the menu and opens the right page. With reduced motion, the backdrop reports moving=false and all three header FloatLayers report quiet=true/active=false.
- Limits: mobile layout and navigation verified through live DOM/interaction; in-app browser emulated screenshots have compositor scaling artifacts, so pixel-level mobile visual approval remains open. No full production build claimed.

### RF-U-002 — Sidebar scroll indicator jumps and sticks

- Source: user scrolling left documentation navigation on /docs/.
- Status: verified locally. Owner: controls_quality; independently rechecked by coordinator.
- Symptom: content scrolls but custom thumb jumps, sticks and does not track consistently.
- Causes: (RF-C-001) Radix calculates an 18px minimum thumb while CSS forces 36px, so painted geometry and travel disagree. (RF-C-002) transparent hover/selection layers retain removed rows' coordinates and preserve thousands of pixels of false overflow after filtering.
- Affected paths: registry/cojeev/styles/scroll-area.css, registry/cojeev/motion/flow.ts, registry/cojeev/motion/flow-motion.ts, tests/rapid-fix-scroll.test.ts.
- Fix: align CSS thumb minimum with Radix, release hidden highlight geometry, clear removed targets promptly and cancel obsolete fade completions when a new highlight appears.
- Verification: coordinator repeated End-to-bottom then search button on /docs/. Full list scrollHeight=6987px, thumb=18px and bottom=460.92px within rail bottom=465px. Filtered list scrollHeight=365px, scrollTop=116px, thumb=164.41px and four matching links visible. Agent additionally verified dragging, no-results/recovery, resize, reduced motion and combobox/ButtonGroup consumers. Fourteen focused tests passed, including three new regressions.
- Related: RF-C-001 and RF-C-002 in controls.json are the two causes of this report, not duplicate user issues. Horizontal-thumb and physical touch coverage remain open.

## Round 2 — Preview, code and control presentation

User report: six screenshots of /docs/button/ (12:07–12:08). Preserve prior fixes; these are new visual/interaction defects, not duplicate RF-U-002 scroll reports.

### RF-U-003 — Preview frame and design-system drift

- Owner: coordinator. Status: verified locally; visual approval remains with user.
- Route/reproduction: /docs/button/, inspect both upper frame corners and switch Preview/Code.
- Cause: a square toolbar painted over a 16px-radius frame with visible overflow; the canvas added an unnecessary nested Card surface.
- Fix: continuous paper surface, correctly inset rounded toolbar, library pill tabs, a plain preview canvas and one shared action row. Mobile toolbar uses two rows: tabs/copy, then motion/reset.
- Paths: registry/cojeev/ui/preview.tsx, registry/cojeev/styles/preview.css, app/docs/docs.css.
- Verification: live toolbar radius 15px inside 16px frame, no square painted corners, Code/Preview and reset work. Fresh button docs checks pass at 360/1440px in light/dark. Final 360px light screenshot inspected: toolbar scrollWidth=clientWidth=326px and document width=360px; every action fits within the frame. Desktop screenshot and checks also pass.
- Evidence: output/playwright/round-2-button/results.json; output/playwright/round-2-button-toolbar/results.json and button-360-light.png.
- Independent review caught a related lower-corner regression in Code mode: the embedded CodeBlock radius=0 rule outranked Preview's nested-layer override. Root reproduced 0px bottom corners, added a failing docs-gate assertion, then moved the host-specific radius rule into the matching layer. Live bottom radii now measure 15px; the assertion and all four responsive/theme layouts pass. Evidence: output/playwright/round-2-corners-before/results.json (expected failure) and round-2-corners-after/results.json. The review found no Critical or Important issue in its bounded preview/source-generation scope.

### RF-U-004 — Code viewer chrome and native scrollbars

- Owner: data_quality; integration coordinator. Status: verified locally with explicit clipboard limits.
- Route/reproduction: /docs/button/ Code tab and /docs/code-block/ long/wrapped snippets.
- Cause: pre owned native overflow and CodeBlock always added standalone header/copy controls inside an already tooled Preview.
- Fix: shared ScrollArea for both axes, token-based paper/ink and language label, embedded mode removes duplicate chrome. Pre remains selectable text; the named viewport owns scrolling.
- Verification: 3 new component tests failed before repair and pass afterward. Root verified embedded=true, zero nested headers, one Copy code action, both custom rails and keyboard End scrolling. At 390px viewport, document width stays 390px; wrap/unwrapped recovery passes. Agent verified horizontal ArrowRight and light/dark surfaces. Copy success feedback passes; exact live clipboard readback was denied/empty through the browser bridge and is not claimed as verified.
- Details and related pattern: [round-2-code.json](round-2-code.json). Registry dependency/public payload refresh remains blocked by RF-S-002.

### RF-U-005 — Variants hidden behind dropdowns

- Owner: coordinator. Status: verified locally for shared rendering and Button behavior.
- Cause: docs exposed only the currently selected variant/size specimen through two selectors.
- Fix: remove Variant/Size dropdowns; render one full interactive specimen plus labelled live Variants and Sizes galleries. Dimensions are shown independently, not a Cartesian duplication. Button's compact galleries avoid repeating the duration/outcome form.
- Paths: components/component-preview.tsx, components/examples/types.ts, app/docs/docs.css, scripts/check-docs.mjs, tests/docs-preview.test.ts.
- Verification: 2 rendering regressions pass, including no empty galleries for a component without alternatives. Button shows all 7 variants and 3 sizes directly (11 total specimens with the main example). Updated docs gate checks every expected gallery entry, real interactions, reset and responsive containment.
- Limit: the shared layout applies across the catalog, but this does not claim fresh behavior/variant coverage of every component. Group audits retain their exact scope.

### RF-U-006 — Menu selection/hover clutter

- Owner: controls_quality; independently checked by coordinator. Status: verified locally for reported Select interaction and related Combobox recovery.
- Cause: legacy pink ::after dots duplicated checks and became an extra grid child; automatic menu adornments stacked a glyph on a blob; pointer-highlighted Select rows inherited a keyboard outline.
- Fix: remove obsolete dots and default extra checks (explicit checks remain opt-in), use one menu identity decoration, suppress pointer-only outlines while retaining keyboard rings. Preserve the existing moving navigation background; committed value and current keyboard candidate remain distinct state.
- Verification: 12 focused tests pass. Root live light/dark Select checks: rows 42px, no dot/check, one glyph/no silhouette, pointer outline none, keyboard 2px solid ring, disabled option skipped, Enter commits a new value. Combobox no-match/clear recovery restores four equal 46px rows with one glyph each and no default checks; ArrowDown/Enter commits Project ideas while retaining distinct chosen/navigation state beforehand.
- Related: Select, Combobox, Dropdown Menu, Context Menu, Menubar share menu-only decoration normalization; checkbox/radio state marks and non-menu combined artwork are preserved. See [round-2-menus.json](round-2-menus.json) for per-family test scope.

### RF-U-007 — Loading button surface and duration

- Owner: motion_quality. Status: verified locally.
- Cause: the demo applied native disabled together with loading, so unavailable styling correctly overrode the busy variant. Its fixed 650ms timer was too short to inspect.
- Fix: use loading without native disabled, retain duplicate-action guards, add 0.5–10s duration slider (3s default), inline success/error outcomes and cancellation. Compact gallery specimens remain interactive for 3s. Docs usage no longer recommends disabled for a busy example.
- Verification: all 7 variants preserve their resting fill/ink while busy in both themes. Duration endpoints, cancellation, retry, duplicate clicks, cleanup/reset and compact duration pass a real Chromium regression. Four Button docs cases (360/1440px x light/dark) pass with zero failures.
- Details: [round-2-button.json](round-2-button.json); final two-row toolbar screenshots in output/playwright/round-2-button-toolbar/.

Implementation order: reproduce and add focused regressions; parallel scoped leaf fixes; integrate Preview/gallery and embedded CodeBlock; batched desktop/mobile/theme/keyboard checks; update exact evidence and any remaining limits. No commit, publish, duplicate-file cleanup or broad unrelated redesign.

## Round 3 — Terminal presentation and action icons

User report: installation command screenshot on /docs/button/ (13:36), asking for proper terminal presentation and icons on actions. Scope: shared documentation controls and copy actions; live component specimens keep their intended variant demonstrations.

### RF-U-008 — Installation commands should look like terminals

- Owner: coordinator. Status: verified locally.
- Cause: InstallCommand used an ordinary paper CodeBlock with the word Terminal as a language badge; it had no terminal-specific surface or prompt.
- Fix: add opt-in CodeBlock variant="terminal", using the existing fixed dark structure/text tokens in both themes, a compact title bar with decorative window marks and terminal glyph, and a separate $ prompt. InstallCommand opts in across getting-started and component documentation. Source-code blocks retain their paper treatment; no fabricated output or executable terminal is implied.
- Paths: components/install-command.tsx, registry/cojeev/ui/code-block.tsx, registry/cojeev/styles/code-block.css, data/component-guides.json, tests/code-block.test.ts, scripts/check-docs.mjs.
- Verification: terminal rendering regression failed before implementation and passes after. The prompt/window marks are aria-hidden, only Copy command is interactive, Unicode/whitespace/source text stays exact. Real browser gate copies both install commands and the full example, compares actual clipboard bytes to displayed code, and checks the success icon. Custom horizontal scroll containment and 390px terminal screenshot verified; dark structure rgb(20,23,27), code/copy ink rgb(249,250,252). Fresh 360/1440px light/dark Button documentation checks pass.
- Evidence: output/playwright/round-3-terminal/results.json, output/playwright/round-3-terminal-final/results.json; output/playwright/docs-button-install-390-light.png. No clipboard denial/fallback simulation in this round; existing fallback implementation unchanged. Registry/public payload refresh still blocked by RF-S-002.
- Related: RF-U-004 supplies the shared code viewer and scroll foundation; this is a new terminal-specific request, not a recurrence of native scrolling or duplicate chrome.

### RF-U-009 — Documentation actions need consistent icons

- Owner: docs_action_icons; copy-control and integration coordinator. Status: verified locally.
- Cause: documentation controls mixed text-only actions with a few illustrated actions, and CopyButton supplied no visual action/status cue.
- Fix: existing decorative AnimatedIcon glyphs beside unchanged labels: copy/check/error or pending copy state; eye/code tabs; refresh reset; book-open guide; menu/x browse and close; x clear search and motion close. Functional disclosure chevrons remain. Shared CopyButton also covers Copy guide, Copy code and Copy selection settings. No automatic decorations were injected into every registry Button.
- Paths: components/component-preview.tsx, components/component-handoff.tsx, components/docs-shell.tsx, components/docs-motion.tsx, registry/cojeev/ui/preview.tsx, registry/cojeev/ui/code-block.tsx, app/docs/docs.css, registry/cojeev/styles/preview.css, tests/docs-action-icons.test.ts, tests/code-block.test.ts.
- Verification: three docs action render regressions and one copy-icon regression failed before implementation; all pass. Unsupported initial icon names were caught during integration and corrected to supported code/refresh-cw names. Mobile toolbar remains two rows with no horizontal overflow. Visual check caught the old block-layout tabs ignoring icon gap; inline-flex now yields a measured 6px icon/text gap. Root verified Clear search restores empty query and search focus; keyboard Close menu and Close motion settings close their surfaces; icon names are hidden from accessibility text and original labels remain.
- Independent review: identified a new CopyButton asChild regression caused by two Slot children. Added a failing real native-anchor rendering test, then placed the icon inside the supplied element using cloneElement. The native link/href/label remains intact and exactly one copy icon renders; 9 focused terminal/action tests pass after repair.
- Limits: this covers documentation-shell actions and shared copy controls, not an icon retrofit or fresh interaction audit of every catalog specimen. No commit or publication.

## Round 4 — Transient feedback layout shifts

### RF-U-010 — Copy feedback changes control and terminal dimensions

- Source: user screenshots on /docs/dropdown-menu/ (13:48–13:49). Owner: coordinator. Status: verified locally.
- Cause: the shared CopyButton inserted a visible status line into its flex column; its default label changed width. During pending state Button also inserted a second spinner next to CopyButton's existing state icon and changed the gap. A failing browser regression measured mobile terminal height 149.75 → 176.59375px even before success.
- Fix: keep the label and single state-icon footprint stable; move the screen-reader live announcement out of layout. Success remains a checkmark. Failure keeps visible manual-copy guidance in an anchored, out-of-flow panel; it does not grow the containing terminal.
- Paths: registry/cojeev/ui/code-block.tsx, registry/cojeev/styles/code-block.css, tests/copy-layout.browser.mjs.
- Verification: Chromium at 360/1440px, light/dark. Command, guide and preview copy controls retain exact width/height through pending and success; terminal height and following-content offset do not change. Default CodeBlock copy dimensions remain stable. Actual command clipboard bytes match the displayed code. Denied API plus failed selection fallback keeps a visible, unclipped error message without changing terminal height. Mobile error screenshot inspected at output/playwright/round-4-copy-layout/failure-mobile.png. Nine focused render tests pass.
- Related: RF-U-008/RF-U-009 improved appearance and icons but did not test transient geometry. This regression adds that missing check for all shared copy consumers; it is not a blanket claim of zero layout shifts across the catalog.

### RF-U-011 — Loading-demo actions reflow neighboring controls

- Owner: layout_docs_terra; coordinator independently reran the focused browser regression. Status: verified locally for the full default-size Button demo at 360/1440px.
- Cause: busy/retry label and spinner width changed the primary action, Cancel was conditionally inserted into a wrapping row, and longer status text grew the example.
- Fix: reserve minimum primary-action width and Cancel's slot, keeping Cancel hidden/disabled when unavailable; reserve a readable status area. This does not alter async duration, cancellation or outcome behavior.
- Paths: components/examples/static.tsx, tests/button-layout.browser.mjs. Related RF-U-007.
- Verification: original mobile regression measured the disabled sibling's relative x moving 156.8125 → 170.09375px during pending. After repair, idle → pending → cancelled → error retains the disabled sibling's relative x/y, demo height and status height at both tested widths. Test waits for fonts and morph hydration. Broader size/variant claims require their own measurements.

### Quick Terra audit — source candidates

- Scope: read-only scan of docs/examples and registry UI/styles for unintended action/status expansion, excluding intentional disclosure, search results and chat content.
- ButtonExample: confirmed and repaired as RF-U-011 above.
- RefinedButtonExample (components/examples/refined-controls.tsx): changing save labels and spinner may move its adjacent disabled button. No current documentation caller found; unused-export candidate, not a confirmed live issue.
- InputGroupExample (components/examples/composed.tsx): an unbounded saved address can wrap its receipt and shift the following input. Source-only candidate, not reproduced or fixed in this pass.
- No additional clear registry-level match found beyond CopyButton in this bounded scan. This was not an exhaustive behavioral audit of 160 components.

## Round 5 — Scrollbar appearance, bounded examples and quiet backgrounds

Design/implementation plan (rapid-fire workflow, local changes only):

- RF-U-012: extend the existing scroll primitives with a 2–16px visual thickness, custom color, and organic/rounded/minimal appearances. Preserve a 24px grab area and native/Radix scroll geometry; show live controls in Scroll area docs. Terra owns primitive/style/example/regression files; coordinator wires docs metadata.
- RF-U-013: reproduce the full-width Dropdown trigger and related action examples, then constrain only demonstration actions. Do not globally shrink Buttons or layout components that intentionally fill their container.
- RF-U-014: add a reusable, CSS-only PatternBackground primitive with dots/grid/none, color/spacing/opacity controls and no pointer/semantic content. Integrate faint dots into Preview and a lighter grid into CodeBlock, preserving clean text and terminal chrome.
- Verification sequence: failing geometry/render tests → scoped implementation → combined desktop/mobile, light/dark visual and interaction checks → one correction batch if needed. Preserve prior fixes and all unrelated dirty/duplicate files. No commit, public payload refresh or publication.

Round-5 results:

- RF-U-012 verified: visual thumb default reduced to 6px, configurable 2–16px; CSS color/token, live presets and arbitrary color picker; organic/rounded/minimal styles with native 24px grab rails. Hover tint is painted only within the chosen visual thickness, not across the grab area. Decorative grip dots cannot exceed the selected thickness. Scoped appearance context keeps manually nested horizontal bars consistent and allows explicit overrides.
- Scroll regression caught a 2px parent being reset to 6px by its horizontal child. After repair, actual vertical/horizontal 2px dragging, custom-color hover/active drag, 24px hit areas and distinct rounded/minimal paint pass. Tests: tests/scroll-appearance.test.ts and tests/scroll-appearance.browser.mjs; live controls at /docs/scroll-area/. PageScrollBar shares the props, but this round's browser drag checks targeted the two ScrollArea axes.
- RF-U-013 verified: Dropdown, Dialog and AlertDialog demo grid containers now align actions to their natural width, instead of stretching grid children. Dropdown was initially 294px wide even on mobile; fixed demo triggers remain below 280px. Menu selection still updates the example receipt. Shared Buttons and intentional full-width specimens are not globally resized.
- RF-U-014 verified: new PatternBackground, dots/grid/none with finite clamped spacing/opacity and CSS color. Decorative, pointer-transparent, static and suppressed in forced colors; positioned layers add no layout. Preview uses subtle dots; CodeBlock uses a lighter grid; terminal defaults stay plain. Source text and copy behavior remain exact. The local catalogue has 161 entries, with the new example at /docs/pattern-background/; its default gallery maps to dots without duplicating that treatment.
- Visual evidence: output/playwright/round-5-surfaces/ contains desktop/mobile light/dark Preview and Code frames plus scrollbar-control screenshots. Coordinator inspected the combined surfaces. Tests/docs-surfaces.browser.mjs covers widths, menu action, patterns and overflow; tests/pattern-background.browser.mjs covers all three treatments, live spacing/strength and stable sample height. Copy-layout regressions still pass in both themes and widths.
- Verification: full unit suite, full lint, final changed-file lint and TypeScript checks passed. Selected copied snippets (five components, nine snippets in the source-check run) have zero TypeScript diagnostics. The source-check command still exits nonzero only for the existing semantic-bloom 2 manifest/catalog omissions (RF-S-002); this is not a whole-registry pass.
- Metadata only: registry.json was updated narrowly for PatternBackground and the modified primitives' API/dependencies. Pre-existing duplicate files and all public download payloads remain untouched. No production build or publication claimed.

## Round 6 — Cojeev icon library

- RF-U-015: existing explorer lists 100 of 176 supported names; native feedback falls back to a small generic scale, and explicit draw only animates path elements. Expand using the installed Lucide 0.577.0 canonical SVG pack, preserve existing geometry/aliases/part recipes, and provide family motion for the remaining names. Keep upstream license notices.
- RF-U-016: selected AnimatedIcon activation does not replay reliably. Terra owns the replay lifecycle and focused pointer/keyboard/touch regression; coordinator owns the pack, motion families, treatments, explorer and integration.
- Plan: optional outline/duotone/organic treatments using existing Cojeev colors and shape contours, all-name search with bounded pages, replay/copy controls, and clear motion descriptions. Defaults stay compatible. Verify all geometry and motion targets plus representative desktop/mobile, dark/light, keyboard and reduced-motion behavior. No publication or commit.

Round-6 results:

- RF-U-015 verified locally: 1,722 supported names (1,703 canonical Lucide 0.577.0 icons plus legacy names/aliases), up from 176 supported and 100 discoverable. Existing geometry wins over upstream additions so authored part indices and aliases remain compatible. scripts/sync-lucide-icons.mjs --check proves the complete upstream snapshot and included ISC/Feather MIT notices match the installed package. Snapshot is 377,134 bytes / 78,251 bytes gzip; it is included in the shared Icon module, not claimed individually tree-shaken.
- Every supported name now has tailored or family feedback. The 100 curated action descriptions remain available; new directional, organic, communication, pulse and stroke-trace families cover the rest. This is not 1,722 bespoke animations or an exhaustive visual approval of every glyph. Full-name geometry/target tests exposed alert-circle and alert-triangle referencing a nonexistent part 2; their legacy compound marks now move as their actual geometry allows.
- Added opt-in outline/duotone/organic treatments and current/pink/blue/olive/yellow accent tones; organic uses the existing Cojeev pebble contour. Defaults remain plain foreground icons, so existing menu adornments do not gain a second blob. SVG size/style and strokeWidth remain adjustable. The picker searches all names, mounts at most 24 result tiles, shows compact variant/size specimens, and supports selection, replay, accent changes and exact JSX copying. Inspector descriptions reserve space; copy feedback reuses the non-shifting shared control.
- RF-U-016 verified locally: activation replays without deselecting the tile. Native and AnimatedIcon keyboard/click deduplication follows the event sequence, not a 100ms delay. Explicit presets previously changed their active marker without reliably painting; all AnimatedIcon presets now use the shared owned-SVG painter. Direct Icon draw also uses that painter, including circles and rectangles. Validation retains the selected glyph and adds its confirmation pulse rather than bypassing treatment props with a separate SVG.
- Read-only review found a duration-change timer leak: changing duration during a transient spinner cleared its timeout without retiring replayActive. A separate timer effect now reschedules that replay and reliably stops it. Terra completed the initial replay pass; capacity interrupted its follow-up, so the coordinator completed lifecycle integration and real-geometry tests. Final reviewer reported no additional important findings after inspecting the fix.
- Browser verification: tests/icon-replay.browser.mjs passes real pointer, long Space, Enter, touch, standalone assistive click, repeated pulse/draw geometry, disabled and reduced-motion checks, plus native Icon long-Space deduplication. tests/icon-lifecycle.browser.mjs proves duration changes retire transient loader/spin replays and native circle/rectangle draw restores complete geometry. tests/icon-library.browser.mjs passes complete-name search, pagination, three treatments, exact clipboard contents, stable inspector geometry, no horizontal overflow, and no client exceptions at 360/1440px in light/dark. Shared copy-layout regression remains passing across the same widths/themes.
- Visual evidence: output/playwright/round-6-icons/; coordinator inspected desktop light and mobile dark frames. Full unit suite: 195 passing. Full lint, final changed-file lint and final TypeScript checks passed; final focused icon/action/docs suite: 11 passing. Full copied-source check: 716 snippets, zero TypeScript diagnostics, but exits nonzero for the existing semantic-bloom 2 manifest/catalog omissions (RF-S-002).
- Local documentation and registry metadata now expose the library and its APIs; the shared base metadata includes the new geometry helper. Public download payloads, duplicate files, branches and commits remain untouched. No production build, registry-wide release or publication claimed. Try /cojeev-ui/docs/icon/ and /cojeev-ui/docs/animated-icon/.

## Round 7 — Reference-faithful Motion Drawer

- RF-U-017: current component inherits Sheet's inset, rounded full-height surface and adds a prominent dashed drag button. The reference is a flush 300px drawer (padding 60px 30px 30px, radius 0), subtle 30% scrim, whole-surface horizontal drag, and a moving/fading toggle. The extra Sheet skin and drag strip replaced that identity.
- Plan: preserve title/open/defaultOpen/onOpenChange/side/dir/enableDrag APIs; adapt the MIT UI Layouts source geometry and toggle choreography into a dedicated Radix Dialog surface, retaining focus trap, Escape, background locking and focus return. Keep default navigation simple; no bordered tile links or internal dashed drag strip.
- Four structural variants: default edge navigation; content-sized floating panel; wider layered workspace with independent panes in its example; bottom action tray with vertical dismissal. Width and side remain independent controls, not variants masquerading as structural differences. Expose merge/push/stay toggle behavior and configurable width, drag threshold and spring settings.
- Execution: root owns primitive/CSS, tests and metadata. Example worker owns a dedicated example module plus manifest/export wiring. RED geometry test → implementation → default reference comparison at 1440/768/390 and combined variant/mobile/RTL/keyboard/drag/reduced-motion browser checks → scoped review. Preserve all prior dirty files; no commit, public payload refresh or publication.

Round-7 results:

- RF-U-017 verified locally: removed the inherited Sheet skin and dashed drag strip. Default is a flush 300px, square-corner edge drawer with the reference's spacing, 30% scrim, whole-surface drag and moving/fading toggle. Cojeev typography/colors, a slightly quicker 180/26 spring, inline reusable launcher and Radix modal semantics are deliberate adaptations. MIT source and attribution are retained in reference/motion-drawer/ and the installable module.
- Four independent live examples now appear on the page: default chapter links update the selected chapter; floating pin/notification preferences retain their local choices; stack uses independent chapter selection and outline panes with an Open action; bottom switches the notes between Notebook, Cards and Focus layouts. Each has its own trigger and visible result; width/side are independent API controls. Example source was extracted to components/examples/motion-drawer.tsx and verified to include all helper data/functions.
- Additional regression repairs: programmatic/default-open drawers now measure toggle travel, not only click-opened drawers. A close/reopen within 80ms formerly retained an open, focus-trapped panel offscreen; presence-aware animation restarts now bring it back. The rapid reversal was reproduced RED in the saved API browser test before fixing, then passed and independently confirmed. Short, wrong-direction and controlled-refusal gestures settle back. Native interactive children do not initiate dragging; bottom content keeps vertical scrolling by restricting its drag start to the header.
- Visual correction: documentation hides its floating reporting launcher and page scrollbar while this drawer is present, preventing those page controls from covering the mobile tray. This is docs chrome behavior, not a new global z-index in the library. Desktop light and mobile dark stack/floating/bottom frames were inspected. The final theme checks use persisted app preference and assert real dark panel paint, including after reduced-motion changes.
- Verification: tests/motion-drawer.browser.mjs passes all four variants at 1440/390px in light/dark, actual animated displacement, reference geometry, focus trap/return, Escape, drag/snapback, reduced motion, viewport/content bounds and real actions. tests/motion-drawer-api.browser.mjs passes RTL edges, configurable thresholds, disabled dragging, bottom directional/header-only drag, controlled refusal, 30-row content scrolling at 360×500, custom width, programmatic toggle merge and rapid close/reopen. Existing reference-docs keyboard/pointer/touch journeys pass at 1440/390 after updating their title and whole-surface drag selectors. Upstream was inspected at 1440/768/390; local variant screenshots cover 1440/390.
- Full unit suite: 199 passing; full lint and TypeScript checks passed, followed by final changed-file lint/TypeScript and both browser suites after the lifecycle fix. Four copied examples have zero extraction/type diagnostics. Overall source-check exit remains 1 only for the existing semantic-bloom 2 manifest/catalog coverage issue (RF-S-002). Focused review has no outstanding findings.
- Metadata updated narrowly in registry.json, component-additions, component-guides and reference-effects so future generation retains the new variants, API and dependencies. Public payloads and pre-existing duplicates are untouched; no build/publication or commit claimed. Try /cojeev-ui/docs/motion-drawer/.

## Round 8 — Subtle background collection and live preview choice

- RF-U-018: request to expand the existing dots/grid decoration into eight distinct quiet backgrounds, put them in their own category, and let every Try it user switch or remove the background. Related to RF-U-014's reusable paint layer, but a new collection/selection feature rather than a duplicate scrollbar or layout issue.
- Added separate Dots, Grid, Contours, Weave, Pebbles, Sunwash, Folds and Sprouts components under **Subtle backgrounds**. Each named wrapper exposes spacing, opacity and color with its own source/API/docs entry; all use the shared PatternBackground renderer. Familiar point/rule structures sit alongside closed organic contours, alternating over-and-under threads, irregular outlines, soft radial washes, faceted planes and paired leaves. Geometry is authored locally; outside references informed restraint, not copied assets.
- Preview's Background action opens a bounded, labeled three-column swatch palette with all eight designs plus Plain. Selection only changes the decorative sibling, preserving live specimen state and toolbar/canvas geometry. Keyboard selection/Escape return focus. Code retains its readable surface and disables the background control. The API supports defaultPattern, controlled pattern/onPatternChange, fixed pattern and showBackgroundPicker. Existing copy/reset actions remain available.
- Every new example has a working Add a note action and independent spacing/strength sliders. PatternBackground's gallery exposes all eight plus Plain. Paint is static, input-transparent and hidden in forced colors; no animation loop, shader, asset fetch, dependency or global preference storage was added. SVG motifs use pixel-space repeating tiles rather than stretching to the preview height. Sunwash uses a larger edge-transparent tile; non-scaling strokes and paint-only corner clipping keep all surfaces clean without clipping foreground popovers.
- Verification: 221 unit tests pass; full TypeScript, lint and diff whitespace checks pass. tests/background-picker.browser.mjs passes all nine choices, state preservation, stable layout, keyboard/focus, selected background across tabs, Code-disabled behavior and no client errors at 1440/390px in light/dark. Existing pattern browser regression passes spacing/strength, nine gallery entries and viewport bounds at 360/1440px in light/dark. Final picker frames in output/playwright/subtle-backgrounds/ were visually inspected. Focused independent review confirmed one specimen mount across all choices; its paint-corner clipping finding was fixed.
- Copied-source check: 18 snippets across Preview plus nine background pages, zero TypeScript diagnostics. The overall command still exits 1 only for the pre-existing semantic-bloom 2 manifest/catalog omissions (RF-S-002). Registry metadata was updated narrowly for wrappers, shared descriptors and picker/helper dependencies; public payloads and duplicate files were not changed. No release, production build or publication claimed.
- Independent route QA passed all eight named pages at 1440/390px: correct headings, category/sidebar grouping and API, exact wrapper paint, Add a note from 1 to 2, keyboard spacing/strength changes without losing state, foreground interaction with forced-colors paint suppressed, stable 280px samples and no horizontal overflow/client exceptions. Receipt: output/playwright/subtle-backgrounds/route-qa.json. Coordinator also inspected full-size Contours/Weave samples and mobile Sunwash alongside the light/dark picker captures.
- Local routes: /cojeev-ui/docs/pattern-background/ for the collection, /cojeev-ui/docs/{dots,grid,contours,weave,pebbles,sunwash,folds,sprouts}-background/ for individual entries. Open Background in any ordinary Try it preview, including /cojeev-ui/docs/button/.

## Round 9 — Switchable report stacks, compact choices and Dock

- RF-U-019 verified locally: related to Round7's layered MotionDrawer, but a new interaction contract: separately arriving real cards and persistent labeled selectors, now used for Request a component and Report a bug. The old stack was decorative and reporting used a single draft behind tabs. Added controlled/uncontrolled panels, bounded stagger, inert mounted inactive panes, keyboard/RTL navigation, and independent active-pane scrolling. Reporting now banks both drafts (fields, files, review/retry state) and atomically migrates the prior single IndexedDB draft. Validation, privacy, pins, capture exclusion, receipts and disconnected-service sending restrictions remain intact. Focused stack/API gates and actual drawer matrix pass. The fourth rotated card exposed insufficient peek space, especially in tall viewports; measured deck geometry now caps rotation to the available horizontal/vertical budgets.1440×1905 and900px custom-width checks pass. Fresh reporting route QA passes at1440/390px in light/dark, including close/reload, independent attachments, review preservation and pin Escape return without production submissions.
- RF-U-020 verified locally: the bottom layout choices used a pill-shaped action primitive stretched to118px with outlined idle choices. Added opt-in `Button shape="card"` to the shared component, feeding12px corners into its actual morph paint as well as CSS; the default action pill remains unchanged. Notebook/Cards/Focus now use compact card choices with quiet idle surfaces. HeroButton's distinct organic/capsule shape API explicitly omits the base shape field to prevent an intersection-type regression. RED/GREEN native/morph/asChild/type tests and browser checks pass; bottom examples perform real layout changes.
- RF-U-021 verified locally: new reusable Dock with glass proximity bar, raised shelf and labeled vertical rail, independently structured rather than recolored. Apple contemporary Dock and Leopard-era shelf/Stacks plus Aceternity/Magic UI informed the design; implementation and geometry are local, with no new package or native app launching. Items expose callbacks, active/disabled states and badges; item size32–72, strength1–2 (rail restrained to1–1.12), keyboard/RTL navigation and quiet/touch behavior are supported. Review repaired narrow tooltips, rail overlap, focused edge clipping and max2x vertical clipping. Actual mobile pointer focus had recentered the scroll viewport between press and release, losing the click; a pointer-origin focus guard now preserves targets while keyboard/programmatic focus still reveals items. The guard resets on pointer leave/cancel/release and external blur. Shelf's automatic minimum grid width also clipped the content card; explicit minmax(0,1fr) tracks and a zero-minimum wrapper now keep content within its frame. Focused contract/browser/type/lint, nested painted-bound checks, outside-release lifecycle and stable-source actual docs matrix pass.
- Integration: fresh full unit suite229 passes; full TypeScript, lint and whitespace gates pass.46 copied snippets across Dock/MotionDrawer/Button/HeroButton have zero TypeScript diagnostics; overall extraction gate still exits1 only for the pre-existing semantic-bloom2 omissions (RF-S-002). Registry/API/catalogue/styles/examples are integrated locally, public payloads unchanged. Independent reporting review found no remaining capability/privacy blockers. Final actual Dock/bottom matrix and reporting matrix pass at1440/390px in light/dark, with desktop/mobile screenshots inspected in output/playwright/round-9 and output/playwright/reporting-stack. No release or whole-library completion claimed. Try /cojeev-ui/docs/dock/ and /cojeev-ui/docs/motion-drawer/; open Make it better for the two report cards.
- Local runtime: repeated OS error60 writing existing `.next/dev/static/chunks` source maps stalled the development server. Gracefully restarted only the verified project process with the existing `COJEEV_NEXT_DIST_DIR` option set to fresh ignored `.next/round9-dev-Lss8HG`; routes again return200. Existing cache, dependencies, duplicate files and user browser tabs remain untouched.

## Round 11 — Icon contours, slider movement and honest motion controls (in progress)

Reopens RF-U-015's blob-backed organic treatment and gallery hover composition; adds readable timing/easing, composed search, circular direction-reactive sliders and meaningful step/range/vertical examples. Shared motion controls now expose saved Off and zero ghost strength rather than imply a working visible preview. Fresh Glide and Ink Drop engine behavior was observed before changes; this was not evidence of an engine failure.

Final verification is blocked by macOS offloading edited source files to cloud-only placeholders. No whole-batch pass or publication is claimed. Implemented changes, passing focused evidence, known review gaps and the exact resume checklist are in [Round 11 handoff](round-11-handoff.md).

## Round 12 — Living page artwork and restrained contents hover (locally verified)

- RF-U-026: documentation intro artwork repeated the same three hard-coded shapes on every route, and ShapeArtwork's effect animated each path back to itself. Route identity now seeds a stable three-piece composition with different silhouettes, arrangements, colours and stagger; the shared artwork primitive has an opt-in compatible contour breathe cycle. Float drift supplies quiet autonomous movement and the composition leans no more than 13px toward a nearby fine pointer. Offscreen, quiet and reduced-motion states remain still.
- RF-U-027: ReadingTrail hover painted the complete 44px link surface, reading as an oversized beige slab at enlarged display scale. Hover now keeps the surface clear and uses a 3px text glide plus a short pink line; keyboard focus retains its visible ring and reduced motion removes the glide.
- Eleven focused artwork, ShapeArtwork and ReadingTrail checks pass. A live browser gate verifies different route compositions, an actually changing contour path, nearby-pointer pull, reduced-motion stillness and the restrained contents hover; the live Next route returns 200 and the changed TypeScript files parse through esbuild. Focused ESLint could not start because the locally restored ESLint package is internally inconsistent (`ConfigCommentParser`/`shouldUseFlatConfig`), so no lint, full-repository type, production build or publication claim is made.

## Round 10 — Coherent reporting cards and durable craft rules

- **RF-U-023 — Reporting stack visually rejected; continuity follow-up.** Reopens RF-U-019's visual part. Status: fixed and locally verified, awaiting user visual approval. Shared MotionDrawer now owns one overlapping-file composition with card-owned heading/close and attached labelled handles. The 16:55 follow-up exposed rounded outer body corners beneath square tab ends, countertranslated handles, pointer interception and covered mobile label lines. Joined owning corners, common transforms, six-pixel overlap and a 64px narrow-screen label zone repair the silhouette and hit targets. Final review also caught dark fixed ink on a theme-neutral rear card; paired card surface/ink tokens restore contrast. Twelve screenshot states verify continuous corner paint, alignment, full label visibility, clicks and >=4.5:1 label contrast. Reporting workflow, drafts, keyboard/RTL and focused shared-stack tests pass; these are local evidence, not approval or publication.
- **RF-U-024 — Global scrollbar policy.** Status: implemented and locally verified. Root ScrollbarProvider sets 4px defaults for shared scroll components; native overflow receives a documented theme-coloured fallback, not the organic contour. Stack cards use ScrollArea. A generic max-height cascade initially clipped panes to 240px; stronger card-qualified sizing now fills the available card and reaches its end. Global keyboard/drag/content-growth/no-double-bar and reporting scroll checks pass. No document-wide mutation observer or DOM reparenting is used.
- **RF-U-025 — Repeated design/code rework.** Ponytail and Ponytail Audit installed skill-only; `cojeev-component-craft` created, structurally validated and independently forward-tested against new-variant and repeat-feedback scenarios. DESIGN.md, its sidecar and the creative repertoire preserve the learned rules. Whole-library audit inventories 170 components, directly reviews 29 and explicitly leaves 141 for deeper review. Findings include RTL Carousel behavior, icon retention, settled ParticleText work, API coverage and the existing duplicate-artifact publication blocker; these are not claimed fixed. See `whole-library-audit.md` for evidence and limits.
- **RF-P-010 — Reporting launcher perpetual idle animation.** Fixed and browser-verified: a seven-second infinite decorative loop became one finite hover/focus response. RED checks caught the idle loop and reduced-motion specificity conflict; the reporting matrix now verifies no idle or reduced-motion animation.
- **RF-U-022 — Sidebar redesign.** Reopened by the 17:27 feedback and implemented: inset tonal paper panel, shallow blue edge, explicit persisted desktop collapse, slim brand/search rail, compact settings, and the shared navigation inside mobile MotionDrawer. Root cause: DocsShell never connected Sidebar collapse state and rendered mobile navigation inline. The existing RED browser test now passes collapse/search persistence, article space recovery, modal focus/Escape/outside dismissal, route navigation, responsive crossing and viewport bounds in both themes at 1440/1024/390/320px, including a 600px-high phone. Independent source review caught and resolved a stale desktop route-selection focus flag. Desktop, collapsed, mobile and short-phone screenshots were inspected. This is local interaction/visual evidence; no production build, publication or user visual approval is claimed.

## Round 13 — Shared hover stability, living navigation and readable source

- **RF-U-028 — Pointer/arrow flicker across clickable content.** User reports movement-only flicker in both the in-app browser and Chrome, expanding the original sidebar report to the whole app. Reproduction found hover repaint detaching/reinserting the live morph SVG and removing/restoring its host class; animated decorative icons also became changing hit targets. Shared repaint now samples authored paint without detaching the SVG or changing the live host class. Decorative icon/shape descendants are pointer-transparent inside native controls, while standalone interactive artwork remains untouched. The RED regression recorded three removed morph bodies during hover; the repaired test records none and checks stable native pointer ownership while moving across buttons, links, text and glyphs. This removes observed DOM instability; it is not proof that the native macOS cursor is visually flicker-free. Awaiting the user's live confirmation.
- **RF-U-022 reopened — Sidebar atmosphere and animation.** Removed the flat strip behind navigation by extending the page atmosphere across the viewport. Replaced the tall empty collapsed rail and hard extrusion with a compact pink tool rail. Expanded navigation has softly tinted paper, restrained blue/pink depth and two small shape accents. Both states use the same shared morph engine as Button, on a pointer-inert background rather than the navigation hit surface. Desktop collapse/search persistence and mobile MotionDrawer semantics are preserved. Dark hover contrast was corrected after visual review.
- **RF-U-023 reopened — Stack tab/body continuity.** The shared MotionDrawer now paints each attached tab and body as one SVG outline, including concave shoulders, rather than joining separately bordered rectangles. Focus stays inside the label handle. A narrow-screen overlap caught in independent review was repaired by bounding combined translation/rotation to the slot's available label space and correcting the font token. The regression checks the first and last painted text positions for all four labels in every selected state, not just center clicks.
- **RF-U-029 — Formatted, highlighted code.** Documentation source is formatted at extraction with Prettier and cached; the shared CodeBlock uses bounded Prism tokenization rendered as safe React text/spans. Theme-aware pink/blue/green/ochre syntax, a quiet source caption and a plain default background improve readability without decorating behind the code. Copy returns the exact formatted source shown. Unknown languages and oversized inputs retain a plain-text fallback. Pinned runtime dependencies and registry metadata are updated locally.
- Verification: `tests/docs-surface-polish.browser.mjs` passes light/dark cursor targets, living navigation, continuous background, formatted/highlighted exact-copy code and desktop/mobile stack joins/full-label hit testing. `tests/docs-sidebar.browser.mjs` and `tests/reporting-continuity.browser.mjs` pass. `tests/motion-drawer-stack.browser.mjs` passes the complete controlled/keyboard/stagger/rapid-reversal suite when run serially; concurrent heavy browser runs exposed timing-sensitive entry samples, so those failed runs are not counted as passes. Six CodeBlock unit tests and full TypeScript checking pass. Updated mobile stack and dark hover screenshots have no remaining blockers in independent review.
- Scope: local source and running docs only; no commit, production build or publication. Eight copied snippets have zero TypeScript diagnostics, but the extraction command still exits nonzero for the existing `semantic-bloom 2` manifest/catalog omissions. Registry regeneration remains blocked by RF-S-002; public download payloads are unchanged and the duplicate files remain untouched.
- Final shared-hover verification: `tests/hover-hit-stability.browser.mjs` passes. `tests/icon-lifecycle.browser.mjs` passes timer/geometry restoration. The older replay gate sampled one fixed 80ms instant and assumed a shorter default duration; it now separates the completed reveal/hover from press, records actual partial stroke frames, and holds Space beyond the current 0.7s duration. `tests/icon-replay.browser.mjs` then passes real pointer/keyboard/touch/assistive activation, pulse/draw, disabled and reduced-motion behavior. No icon runtime change was needed for this check.

## Round 14 — Existing backgrounds connected to Preview

- **RF-U-018 reopened — Preview still offered only the original patterns.** Source: user screenshots on `/docs/depth-background/`, 19:48–19:49. Status: verified locally; visual approval remains with user. Cause: Preview's value type, picker inventory and renderer only supported PatternBackground. Existing PigmentField, DepthBackground and AmbientBackground were never connected to that shared contract.
- Fix: one shared background inventory and paint adapter expose seven existing treatments (Pigment wash, Pollen, Depth contours, Orbital, Ambient drift, Ambient orbit, Ambient contours) alongside the eight original patterns and Plain. Swatches are static CSS/SVG; only the selected living field is lazily mounted. The field is an inert sibling, not a wrapper around the specimen. Preview lowers pattern strength and living-field opacity to protect text, and background-component documentation starts on Plain so its own sample is not layered over extra dots. No new background engine, texture asset or dependency was created.
- Verification: the initial new-library browser regression failed because Pigment wash was absent. After integration, all seven choices pass mounted-renderer, no-old-dots, stable DOM/state/dimensions, quiet motion and bounded picker checks at 1440px light and 390px dark. Dark mode is asserted from the actual root after operating appearance controls, not inferred from storage. A normal-motion case observes changing Pollen transforms and running Ambient drift, then verifies reduced-motion stillness and forced-colors paint suppression. The legacy nine-choice regression also passes keyboard/focus, tab persistence, Code-disabled behavior and light/dark at 1440/390px. Evidence: `output/playwright/preview-background-library/` and `output/playwright/subtle-backgrounds/`.
- Integration review caught a lazy-import relocation defect in the registry writer. The shared import rewriter now handles `import(...)` as well as static imports. Both new tests reproduce failures with the old rewrite and pass with the repaired implementation against the actual Preview helper source. Registry dependency/helper metadata is updated narrowly. Independent final review found no remaining blockers in source or regenerated mobile dark screenshots.
- Focused rendering tests: 9 pass. Full TypeScript check and whitespace check pass. The Preview copied-source check has zero TypeScript diagnostics; its overall exit remains 1 for the pre-existing `semantic-bloom 2` catalog/manifest omissions (RF-S-002). Public payloads are unchanged; no production build or publication claimed.
- Separate observation: on a fresh mobile route the appearance control is inside a closed navigation drawer, so seeding a stored dark preference alone did not establish a dark root in the test. Opening navigation and explicitly selecting appearance does. This round verifies backgrounds in actual dark mode; it does not claim a repair to theme bootstrap.
- Gate limit: scoped ESLint produced no output for more than two minutes and its owned process was stopped; no fresh lint pass is claimed. The two import-rewrite regressions live in `tests/registry-imports.test.ts`, included by the normal unit-test file pattern, and pass independently.

## Round 15 — Ghost hover cancellation repair

- **RF-U-030 — Context Menu ghost remains between rows.** Source: user's 19:53 screenshot on `/docs/context-menu/`. Status: verified locally for this interaction; no publication or whole-library claim. Related to RF-U-006's menu clutter, but a distinct shared motion-lifecycle defect rather than duplicated CSS decoration.
- Cause: a selection change starts the pointer ghost's fade, then `clearPhases` stops every painter lane, including that fade and its geometry cleanup. The live RED test reproduced the screenshot: Duplicate's ghost remained at opacity 0.982 while Pin note was selected. An independent deterministic painter test failed with opacity 0.622 after selection interruption.
- Fix: painter interruption can target selection/trail independently of hover. Selection phase changes no longer cancel hover cleanup. Full group suspension and removal explicitly clear the ghost, preventing a pending hover animation from writing styles back after Flow Off. No consumer-specific paint override, new motion engine or dependency was added.
- Verification: both regressions pass after the shared fix. Twelve focused menu/flow unit tests pass; full TypeScript checking passes. `tests/context-menu-ghost.browser.mjs` passes 1440/390px light/dark checks across Glide, Ink Drop and Rubber, including rapid reversal, heading/outside exit, keyboard navigation, reduced motion, Flow Off/re-enable, close and keyboard reopen. A positive shared Motion Settings tab test proves ghost previews still appear on unselected tabs and clear on selection handoff. Desktop and mobile menu captures were inspected under `output/playwright/context-menu-ghost/`.
- Limits: mobile menu captures also expose a separate existing placement overflow (a 260px menu starts at x197 on a 390px viewport, with 193px available). This repair does not change popup placement or claim mobile layout approval. The older `tests/motion-controls.browser.mjs` reaches its responsive screenshot loop after the motion assertions, then fails because its dialog disappears on resizing; that full script is not counted as passing. Scoped whitespace checks pass; the repository-wide check reports unrelated existing trailing whitespace in `components/examples/action-icons.tsx:65`, left untouched. Public registry payloads remain unchanged under the existing RF-S-002 blocker.

## Round 16 — Shared action buttons and a more legible sidebar scrollbar

- **RF-U-031 — Button-shaped documentation links bypassed our components.** Source: user's 19:55 Hero button screenshot. Status: fixed and locally verified for the related-component actions and Getting Started actions. Both were plain links with duplicated button paint and a Unicode arrow. They now compose `Button asChild variant="secondary" size="lg"` with `AnimatedIcon`, preserving native anchor navigation. Documentation CSS owns wrapping and spacing only. The shared Button stylesheet also removes inherited prose underlines from its anchor form. Ordinary prose and navigation links remain links; this is not a claim that every clickable item should be a Button or that the whole library has been exhaustively audited.
- The initial browser regression failed because the actions lacked Button's contract. A ghost-wrapped intermediate did not acquire the actual contour, because ghost is intentionally excluded from automatic morph eligibility. Using the existing filled secondary treatment restored the intended shared motion without changing ghost semantics or adding a consumer-specific animation. A separate underline assertion reproduced the inherited link decoration before the shared CSS correction.
- **RF-U-032 — Sidebar thumb too thin.** The shared navigation ScrollArea now opts into a 6px thumb instead of the global 4px default. Its 24px drag rail is unchanged. Desktop and mobile use this same navigation component; the global default and other scrollbars remain untouched.
- Verification: `tests/docs-button-contract.browser.mjs` passes actual contour movement, stable hit areas and pointer ownership, shared icon/native-anchor composition, no underline, keyboard navigation and reduced-motion stillness on Hero button and Getting Started. `tests/docs-sidebar-scrollbar.browser.mjs` passes measured thumb/rail sizes and real drag scrolling at 1440px and 390px. Light/dark captures were inspected, including mobile dark after theme transitions settled. Full TypeScript checking and ten focused Button/API/preview/action-icon tests pass. Scoped whitespace checks pass. Screenshots: `output/playwright/docs-button-contract/` and `output/playwright/docs-sidebar-scrollbar/`.
- **RF-U-033 — Hero button variety.** Status: design proposed, awaiting user approval; no new hero treatments implemented in this round. Proposed twelve tactile treatments: Liquid, Split Arrow, Paper Fold, Pressed Key, Ribbon, Ticket, Seal, Ink Sweep, Orbit, Sliding Window, Stacked Paper and Spotlight. Each needs a distinct silhouette or interaction, not a size or color alias, while retaining shared Button semantics and quiet-motion behavior.
- Scope: local source and running documentation only. Public registry payloads remain unchanged under RF-S-002; no publication, production build or user visual approval is claimed.

## Round 17 — Compact navigation ribbon and seamless theme glyph

- **RF-U-034 — Theme icon briefly acquires a square outline.** Status: fixed and locally verified. This was not keyboard focus: pointer activation had `:focus-visible=false` and no CSS outline. The theme reveal cuts a hole in its captured image and repaints a live glyph tile; fractional CSS coordinates antialiased both meeting edges and exposed a rectangular seam. The RED pixel test measured 53–56 channel levels of discontinuity around the glyph. The shared theme-transition helper now aligns the mask and replacement tile to physical pixels while preserving the icon's exact internal position. Both directions measure zero discontinuity in the sampled glyph boundary at 1x and 2x. Actual focus-visible, sun/moon motion and reduced-motion behavior remain intact.
- **RF-U-022 reopened — Useful collapsed navigation.** The user approved a readable narrow index and its written design. The 148px ribbon keeps theme, colours and motion controls available beside search and full expansion. Its bottom disclosure grows the same object vertically into the existing categorized component list; up/Escape folds it, and route changes keep compact mode. It reuses the same SidebarContent/ScrollArea tree, catalogue and shared settings stores. Full names wrap instead of inheriting the base mini rail's hidden labels. The shared navigation surface supplies restrained tonal/artwork paint. Sidebar theme is now icon-only, without changing ThemeToggle's public default.
- Browser coverage: `tests/docs-compact-navigation.browser.mjs` passes light/dark theme changes from compact mode, working colours/motion overlays and focus return, more than 160 real links, stable compact width, vertical expansion, real route navigation, rapid reversal, Escape folding, nested-overlay Escape ownership, reduced motion, last-entry access at 560px height, and mobile drawer behavior. An additional live UI check disables Enable motion in the real Motion settings sheet and verifies immediate 696px extension in a 720px viewport (`transitionDuration=0s`), then restores the setting.
- Visual review caught a pale inherited sidebar glyph on the blue compact Motion button; the existing docs motion treatment restores its paired ink. Its browser assertion failed before correction and now passes. The actual compact, extended, short-height and mobile frames were inspected in `output/playwright/docs-compact-navigation/`; the theme mid-transition frames were inspected in `output/playwright/theme-glyph-seam/`.
- Existing sidebar persistence/search/desktop-mobile resize/focus tests and 1440/390px scrollbar drag tests pass. Ten focused theme geometry, appearance and docs action-icon tests pass. Full TypeScript checking and scoped whitespace checks pass. These are local Chromium and screenshot results, not exhaustive cross-browser or user visual approval. No production build, commit or publication; public payloads remain unchanged under RF-S-002.

## Round 18 — Library-wide craft review intake

- Status: open; source investigation and proposed first-batch design only. No UI fixes, new variants or library-wide verification claimed in this round.
- The owner's September 10 20:51–21:07 screenshots and component inventory are retained in [the full overhaul intake](2026-09-10-library-overhaul-intake.md). It separates actual capabilities, appearance parameters, behaviours and documentation failures; every named family has a repair/design checklist.
- **RF-U-003 and RF-U-005 reopened:** the Try it toolbar still uses accidental wrapping, and metadata-driven galleries still mix colours, sizes, shapes and unavailable/error states with structural variants. Earlier containment/visibility passes did not constitute visual acceptance.
- Shared scrollbar coverage is reopened beyond the native fallback boundary: API tables and other owned scrollers still require real organic-scrollbar integration. Preserve RF-U-004's completed CodeBlock work and previous managed-scroll tests.
- **RF-U-035 — Form-family capability and appearance gaps:** source confirms Calendar is single-date only and Slider has no rubber appearance. Input/InputGroup/DatePicker/Textarea have divergent corner owners. The intake proposes a shared form/radius contract and explicit range/multiple and rubber work; implementation is pending design approval.
- **RF-U-036 — Icon studio colour control is ineffective in Outline:** reproduced in a separate local browser. Blue leaves Outline ink unchanged (`rgb(38,40,45)`); switching to Organic makes Blue/Pink produce different ink (`rgb(149,186,232)` / `rgb(240,164,204)`). Source treats tone as an accent, while the default treatment has no accent layer. Proposed repair separates foreground/accent controls and truthful copied output rather than changing every existing action icon's public contract.
- Additional source findings: compact sidebar hides Work with me/GitHub header links; ToastExample's grid stretches its trigger; chart demos use NativeSelect; DataTable already composes Table. Original Linear Modal reference was located and inspected, but its animation failure has not yet been reproduced.
- Recommendation offered: shared preview/forms first, with hero-first or behaviour-first as alternative sequences. The full inventory stays in scope whichever is chosen. Existing working-tree changes and RF-S-002 registry-publication boundary remain untouched.

## Round 19 — Compact palette arrangement polish

- **RF-U-022 refinement — Compact ribbon composition.** Source: owner's 21:13 screenshot and explicit endorsement of the current direction. Status: refined and locally verified; final visual approval remains with the owner. The broad Round 18 overhaul remains open and is not claimed as implemented.
- Cause: the header used a different distribution from the 44px tool grid; Search had no resting surface, Motion dominated with a solid accent, and the index arrow was replaced on each toggle. The bottom action lacked a clear attached surface.
- Fix: brand/expand now align with the two tool columns; all controls retain 44px targets. Search, Motion, theme and colour controls have paired, quiet tonal faces. The bottom pull-down has an attached surface and a persistent rotating chevron; full expansion uses a sideways arrow. Two fine contour echoes and the existing flower remain behind the content, pointer-transparent. Existing shared surface/button morphs, settings stores, width, index and mobile drawer behaviour are preserved.
- Regression evidence: `tests/docs-compact-polish.browser.mjs` initially failed “Brand and first tool column must align.” It now passes in both modes for column/glyph alignment, target size, edge-to-edge stable hit ownership, glyph continuity, rapid reversal, quiet mode and focus. The existing compact-navigation regression also caught a mismatched dark-mode Motion glyph inherited from a solid-accent rule; a narrowly scoped ink inheritance repair fixes it.
- Fresh checks: `tests/docs-compact-navigation.browser.mjs`, `tests/docs-sidebar.browser.mjs` and `tests/docs-sidebar-scrollbar.browser.mjs` pass theme/settings, route/persistence, compact expansion, short-height scrolling, mobile drawer/focus, and actual 24px scrollbar drag targets. TypeScript and scoped whitespace checks pass. The layout detector reports no findings; that is not a visual approval claim.
- Visuals inspected: light/dark compact and extended ribbons under `output/playwright/docs-compact-polish/`, plus short dark and mobile light under `output/playwright/docs-compact-navigation/`. The inspection produced one correction batch for icon paint/size. Evidence is local Chromium, not an exhaustive palette/browser audit or a claim that all native-cursor issues are resolved. No publication, registry refresh, cleanup, commit or push.

## Round 20 — Approved foundation batch and quieter discovery

- Source: owner's full-overhaul approval, 22:24 sidebar simplification request and explicit lossless duplicate-backup approval. These supersede the earlier compact icon grid. Status: bounded foundation scope implemented and independently approved; the [full intake](2026-09-10-library-overhaul-intake.md) remains open.
- **RF-U-003 / RF-U-005:** deliberate Preview/Code/Copy hierarchy and responsive tools shelf; approach/size/corners controls; sizes no longer repeated as pseudo-variants. Mounted specimens retain drafts through Code, configuration and background changes; explicit Reset alone restarts them. Copy contains the selected configuration, not arbitrary internal draft state. Three field approaches and selected Slider guidance are authored; classification/purpose captions are not yet universal.
- **RF-U-035:** shared Contour/Editorial/Inset fields and inherited/local radius contracts; value-dependent Rubber Slider; native single/range/multiple Calendar and DatePicker; Checkbox/Radio row/card/chip and Switch capsule/rocker/latch. Existing native form, keyboard, ref and legacy indicator APIs retained. Review caught three choice issues, then approved the corrected stationary switch root, standalone shared styling and explicit radio appearance composition.
- **RF-U-022:** calmer expanded-default navigation, compact labelled tools, persistent Work with me/GitHub, concise Search library, folding categories/counts and delayed hover/focus component previews. Contour strings/extra artwork removed from the rail while gradient/shared motion remain. Icon/Shape lead Foundations; Charts is grouped separately. Mobile stays MotionDrawer and does not require hover. This supersedes Round19's two-column decorative grid; no all-browser native-cursor claim.
- **Search:** shared Command now uses Fumadocs static search over titles, descriptions and guide/API text, with retained queries, keyboard results, loading/empty/error/retry and focus restoration. Supplied shortcut no longer acquires a second wrapped Enter cue. Independent review forced index503 and confirmed retry recovery. The static route is locally verified, not a production-export certification.
- **Scrollbar boundary:** TableContainer, API tables and the reading specimen now use actual ScrollArea, including explicit RTL viewport direction, refs/scroll callbacks, keyboard and thumb drag. [Coverage audit](global-scrollbar-audit.md) lists remaining native owners. DataTable pagination and full global migration remain open.
- Evidence: Tasks1–8 reports under `.superpowers/sdd/2026-09-10-overhaul-foundations/`; full TypeScript pass; 62 copied snippets across12 components, zero diagnostics; six workbench theme/width cases; Calendar/DatePicker24 actual-docs cases; choice36 actual-docs cases plus native enabled-motion, legacy and standalone CSS checks; sidebar320/390/1024/1440 and short-height checks; search390/1280 both themes. General `check-docs` was updated from the obsolete size-gallery/filter contract and passes all three choice pages at390 light/dark, exact copy, behavior and chrome/search checks.
- Visual/source independent review approved the bounded batch. Captures: `output/playwright/sidebar-calm/`, `docs-search/`, `overhaul-workbench/`, `overhaul-rubber/`, `choice-foundations/`. The owner has not yet approved these final visuals. No commit, push or publication; remaining families are not silently marked done.
- **RF-S-002:** seven exact duplicates moved losslessly to `/Users/sanjaykumar/Documents/ChatGPT/cojeev-ui-duplicate-backup-20260910-vraxPK`; every SHA-256 matches. Canonical files and unrelated reference copies retained. Local registry rebuild and generated-content verification are tracked below; backup is complete, rebuilding is a separate gate.

## Round21 — Primary-agent recovery after rejected overhaul

Latest continuation: Carousel/Reading Trail and Alert/Progress add four locally verified rows (39 total;16 plus shared-shell work still open). Carousel preserves selection/saves across Shelf/Story/Index and corrects native bounds, inherited RTL,44px dots, rapid reversal and false single-slide overflow. Reading Trail preserves native document/local contracts across Spine/Bookmark/Overview; its reproduced3px link-hover translation is removed, and the reader now owns an opaque paper surface. Alert retains six statuses independently of Note/Banner/Dispatch, with one morph stamp and a44px non-rotating dismissal target. Progress removes repetitive comparison nesting and retains every original track choice. Personal screenshots caught stale100x20 SVG coordinates producing1px segments: initial usePaintSize measurement now runs after MotionValue subscriptions, with cleanup. Actual rendered geometry passes; shared Slider/Rubber/Questionnaire regressions were rerun. Full exact evidence, representative personal images, catalogue/native/quiet/copy gates and source-matched affected downloads are recorded in the recovery delivery document. Prior checkpoints below are historical; no whole-library or owner-aesthetic approval is inferred.

- Status: twenty-seven component rows implemented and locally verified across the checkpoints below, not whole-library completion. The owner explicitly requested primary-agent re-verification; no delegation was used in this recovery. The historical Round20 independent approvals do not replace owner visual acceptance.
- **RF-U-005 / RF-U-035:** Checkbox and Radio expose all six original glyph shapes and selected-mark/show-hide controls separately from Row/Card/Chip. Real docs now preserve values while changing those controls. Removed competing choice-card illustrations; Row hides no-op corners and Card/Chip name Container corners accurately. Native form, keyboard, disabled, ref and controlled-refusal checks remain covered.
- **RF-U-033:** delivered twelve Hero Button concepts plus retained Capsule, rather than counting sizes or palette changes as new concepts. The real gallery, each appearance, disabled/loading/link/ref semantics, stationary hit rectangles, shared quiet settings and copied code were checked. A size-rule regression initially flattened Seal; a geometry test reproduces and prevents it.
- **Accordion/Card/Collapsible:** three useful approaches per component, real local actions and a shared reversible disclosure-height owner. Dedicated `asChild` tests cover a compatibility regression caught and corrected during implementation. CardAction's40px default-token assumption was corrected to an explicit44px minimum after the stricter live test failed.
- **RF-U-013 / Toast:** corrected the stretched demo trigger at its container boundary. Compact/Actionable/Receipt arrangements have local save/Undo, focused initiation,44px dismissal and repeat-open behavior. Semantic receipt ink is checked independently from the cream example palette.
- Evidence and scope: [recovery delivery](2026-09-11-recovery-delivery.md). Seven real docs routes;125 captured images with representative primary visual inspection;90 copied snippets with zero diagnostics; full TypeScript and focused lint;173-item registry build; all seven affected installed dependency closures match the original checkout after delivery. Thirteen affected/dependency payloads refreshed, unrelated entries preserved. No production build, publication, commit, push, all-browser cursor claim or full55-row closure.

## Shared issues

### Round21 continuation — charts, navigation and Tables

- Charts introduction plus six chart families: real Cojeev dataset selector, Analysis/Brief/Ledger compositions independent of retained geometry modes, numeric missing/zero/scale correctness, bounded custom-scroll values. Category metadata now agrees with the Charts discovery group. Seven chart geometry tests and seven actual routes checked.
- Pagination/Tabs/Navigation Menu: fixed hit rectangles and numeric state, useful navigation approaches, one tab-content position and closed-focus isolation, Escape cancels pending hover reopen. Appearance changes no longer erase the shared Flow marker and turn decorative paint into layout items. Immediate tab ink avoids theme-transition flashes. No whole-app cursor certification.
- Table/DataTable: one discoverable guide, both APIs retained, native Ledger/Rich/Comparison examples. The page flicker was reproduced with five simultaneous old/new rows, then fixed by removing row-exit overlap. Checked row IDs, sorting/filtering, empty recovery, nested actions, controlled state/ref, visible keyboard focus and true horizontal custom scrolling.
- Current combined evidence: eighteen intake rows / nineteen example IDs,137 copied snippets, zero TypeScript diagnostics, full source type check, focused lint,173 registry items rebuilt, source-verified33-payload closure. Served4321 files equal delivered originals. Added44 navigation and12 table images;254 captures retained overall. Primary visual inspection remains distinct from screenshot capture and owner approval. Full55-row overhaul, remaining37 rows and shared-shell acceptance are open.

Round21 selection-control continuation: Calendar/DatePicker catalogue modes restored after a failing real-docs check; Calendar contour now changes finitely instead of merely moving. Native mode contracts, Clear/Done/Escape and focus verified. Rubber measured at roughly15px versus1.875px waist, with drag/range/RTL/vertical/quiet checks. Switch's three mechanisms personally inspected, native and actual-docs checks pass. Current recovery:22 rows /23 example IDs,156 copied snippets,37-payload source-matched closure,40 additional images. Remaining33 rows and shared-shell acceptance stay open. Full evidence is in the recovery delivery document.

Round21 form continuation: five actual docs now distinguish Input surface, Field composition, joined InputGroup jobs, Textarea writing/composer/limited response and Label semantics. Values/errors/ref/form/native editing persist. Clear's1px active transform was reproduced and removed for stable-hit Buttons. TextareaScrollArea attaches the real organic thumb to the native editor, with forced-colour/horizontal native exceptions. Native, actual-docs, shared-scroll, type/lint and186 copied-snippet checks pass. Current checkpoint:27 rows/28 example IDs;40 source-matched payloads;60 form and24 foundation captures. Remaining28 rows and shared requests stay open; exact evidence/limits in the recovery delivery document.

Round21 artistic recovery and continuation: Accordion's explicit motion opt-outs were reproduced; FAQ/Chapters/Editorial now deliberately enable shared contour paint and have personally inspected answer-slip, stitched-tab and editorial compositions. Motion/path/held-target/reversal checks pass. MultiSelect/Select/NativeSelect received real structural arrangements and retained native contracts. NumberInput adds bounded Scrub and Quantity; cancelled native reset now remains cancelled. OptionWheel's native wheel handler releases scrolling at its boundaries and retains side placement independently. Questionnaire preserves all selector shapes/marks with Stacked/Journey/Worksheet; native press targets no longer shrink, reset paint agrees with native state, and per-question paint variables remove the duplicated pink band without changing AgentChat defaults. Breadcrumb and Stepper now have three purposeful compositions each, stable targets, validated progress and retained drafts. Personal inspection caught and corrected wrapped question titles, dangling breadcrumb separators, faint/missing step paint and overlong checkpoint labels; stronger actual-paint tests now accompany semantic checks. Latest exact evidence in the recovery delivery document:35 rows,20 remaining, current responsive images and source-matched downloads. RF-U-005/RF-U-028/RF-U-035 remain scoped regressions, not permission to infer full-app cursor or aesthetic acceptance. No sub-agents used.

### RF-S-001 — Registered components missing from navigation

- Source: proactive docs-shell review. Status: verified locally. Owner: coordinator.
- Symptom: Composition, Visual effects and Motion entries exist in the registry but never render in sidebar categories, including filtered search results.
- Cause: lib/categories.ts omits these three category names while DocsShell only renders its listed categories.
- Fix: restore the three categories. Add tests/docs-categories.test.ts to check every registered UI entry maps to a unique navigation category.
- Verification: 162 live navigation links (160 components plus Getting started/Reference effects); Compact dashboard is now reachable through mobile search; regression test passes.

Proactive family findings and exact verification live in controls.json, motion.json and data.json. A source review or default render is not complete behavior/variant coverage.

- RF-D-001: Agent chat now restores draft focus when a focused Stop disappears at completion, preserves focus elsewhere, and keeps Send-to-draft handoff. Live reproduction and repair verified; persistent docs regression assertions added.
- RF-M-001: Orbit images and Infinite spiral now reposition replacement images even when the replacement set has the same count and playback is paused. Effects now track rendered item identity, not only count. Individually live-verified with six replacement images in each.
- RF-M-002: paused reference-field canvases now repaint on palette changes without restarting animation. Shared appearance listeners and cleanup updated; Magic rings live-verified.
- Motion regression script: tests/rapid-motion-regressions.browser.mjs syntax passed. The first combined CUA run timed out on navigation before assertions; do not treat this whole script as passed. Individual issue verifications are recorded separately in motion.json.

### RF-S-002 — Duplicate backup and local registry rebuild verified

- Source: integration check. Status: resolved and verified locally. Owner: coordinator.
- Reproduction: npm run registry:build.
- Result: Undeclared registry helper: semantic-bloom 2. The pre-existing untracked registry/cojeev/ui/semantic-bloom 2.tsx is collected as a new installable component without metadata.
- Owner approved preservation outside the registry. Seven exact files are backed up under `/Users/sanjaykumar/Documents/ChatGPT/cojeev-ui-duplicate-backup-20260910-vraxPK`, preserving paths, differing docs/generated content and matching SHA-256 checksums. README documents restoration; no files were deleted. Unrelated reference copies remain untouched.
- Standard shadcn build now succeeds:171 items (base foundation,66 base components,104 additions). The repository-installed CLI spent25minutes in macOS kernel file reads; a process sample confirmed it was not compiling. An isolated shadcn4.21.0 was materialized from the offline package cache under `/tmp/cojeev-registry-cli-TYT05B`. Its CLI SHA-256 matches the repository copy (`567b635b7fb7659acbeaceaeb013715191d3e6f67273bbc53ce847087d3a030f`). The builder now accepts an optional `COJEEV_REGISTRY_CLI` path while preserving the normal default and arguments. Existing project dependencies and lockfile were not changed by this workaround.
- `scripts/check-foundation-downloads.mjs`: RED old payload missing ControlRadius helper → GREEN14 foundation downloads/109 source files matching rewritten installed content, file targets and shared choice imports. All171 indexed payloads parse, contain files and match the generated catalogue. Independent reviewer approved the optional CLI path and freshness checker. This is payload freshness, not a full stranger-install, static-site build or publication claim.
- Full copied-source gate after rebuild:170/170 default examples and740 snippets, including every documented variant/size/multi-size wrapper, pass with zero TypeScript diagnostics, extraction errors or coverage errors. Receipt: `artifacts/overhaul-all-source.json`. This closes the old duplicate-induced copied-source blocker without asserting that every component's visuals/behavior have been redesigned.
- Final focused lint and scoped whitespace checks pass. Lint used matching isolated ESLint9.39.5/Next config16.3.4 plus the installed Next parser, because the repository dependency reads also stalled there. No project dependency or lockfile changes from that workaround; no full-repository lint claim. Owned stalled/warm-up processes were stopped after successful alternate checks; the development server remained running.

### RF-S-003 — Copied example types drift from live examples

- Source: round-2 integration check. Status: verified locally. Owner: coordinator.
- Cause: components/example-source.ts inlined a hard-coded ExampleProps type, so Button's new compact prop compiled in the app but failed in copied snippets.
- Fix: extract the real ExampleProps declaration from components/examples/types.ts and include both source and shared-type modification times in the snippet cache key.
- Verification: focused source check initially reported 22 TypeScript diagnostics. After repair all 24 Button/Preview/CodeBlock snippets pass with zero extraction or type diagnostics. The overall check still exits nonzero on the two pre-existing semantic-bloom 2 catalog/manifest coverage errors from RF-S-002; it is not reported as a full gate pass.
- Evidence: artifacts/round-2-example-source.json. Pattern: generated documentation types must come from the same declaration as live examples, not a maintained duplicate.

## Session checkpoint

- Registry census: 161 locally documented components after Round 5.
- Local preview listener: port 4320 was restored during final checks; /cojeev-ui/docs/button/ returned HTTP 200 and final browser checks passed. Existing user tabs were not navigated.
- Documentation/user scrollbar, round-2 preview/menu/loading fixes, round-3 terminal/action icons, and round-4 shared copy feedback geometry are locally verified with the limits above.
- Latest round-3 combined automated run: 189 tests passed, zero failures. Full lint and TypeScript checks passed. Final Button documentation checks passed at 360/1440px in light/dark themes, including exact clipboard contents and action feedback. Regression scripts added by agents are tracked with their own execution scope in group records.
- Round-4 verification: 189 unit tests passed during integration; focused copy and Button geometry browser regressions pass. Changed-file lint and final TypeScript checks pass. Terra's remaining long-address receipt candidate is explicitly unverified above.
- Registry refresh blocked as recorded in RF-S-002; no production build or publication claimed.
- Three component audits have partial coverage; consult their group records for precise status.
- Overall audit remains in progress.
